# GitHub dev 汇总发布与 13510 升级

前置条件：用户明确授权先推送 GitHub dev 再升级 13510；工作树干净；13511 已验收前端 `05b36a3` 和后端 `21f23ef`；保留正式 CODEX_HOME 和 CLI 0.153.4，不创建备份。

操作步骤：

1. 获取远端，确认 `origin/dev` 是当前提交祖先；检查全部待推送提交及中文 CHANGELOG。本次为 29 项已有提交加发布记录。
2. 执行 `pnpm run test:unit`，以及 `node -e "process.argv=['node','dist-cli/index.js','--help']; import('./dist-cli/index.js')"`；后者应打印帮助并退出，不启动额外服务。
3. 将 HEAD 快进推送到 `refs/heads/dev`，再核对 GitHub 返回的 SHA 和本地 HEAD 一致。
4. 在 13511 预演 `output/playwright/dev-release-13510-20260907/` 中三个验证脚本；使用 `VERIFY_PREVIEW=1`，不创建新会话、修改配置或发送/停止真实任务。
5. 执行同目录 `deploy.py --prepare`，核对已验收构建、正式与预览目录隔离、配置哈希及发布来源。由目标服务之外的 systemd 任务执行 `deploy.py`；检查其等待回合结束，禁止直接重启承载当前回合的服务。
6. 空闲切换后核对 runtime-info、实际 CLI 子进程和 CODEX_HOME、监听 PID、HTTP 资源、会话集合及切换前后的会话偏好。13511 的进程、资源和配置必须保留。
7. 在真实 13510 既有测试会话执行 1440×900、375×812、768×1024 明暗六组只读页面检查：刷新后独立 Fast 状态保持、加号入口唯一、模型菜单无速度选项、新聊天默认标准。
8. 用前端协议回放在 375×812 明暗两组分别检查完整白/灰状态卡与子任务详情，以及 44×44 正圆停止按钮。回放仅替换浏览器收到的状态，不改变真实线程或执行停止动作。

预期结果：全量单测 69 文件 / 672 项通过；CJS 帮助退出码 0；远端 dev 与 HEAD 无差异；发布回执为 `phase: complete`。浏览器共十组，截图和断言保存在上述输出目录。等待期间或失败状态不能当作升级成功。

清理与回滚：浏览器只修改自己的临时页面和存储，不清理真实聊天；临时浏览器进程退出，systemd 发布任务完成后退出。未创建备份；需回退时从指定提交重建整套前后端，在正式端无任务时按相同流程切换。
