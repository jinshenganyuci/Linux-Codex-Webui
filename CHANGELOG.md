# 更新日志

本文件记录 Linux-Codex-Webui 每次推送中面向用户和运维人员的重要变化。

后续提交与推送统一采用中文标题、中文详细正文和中文更新日志。代码标识符、命令、接口路径以及 Codex 协议字段保留原始英文名称，避免产生含义偏差。

## 2026-09-07（本地版本，尚未推送）

### 正文蓝色链接与代码块展示优化

- **问题或目标：** 用户截图指出列表里的可打开链接不明显；列表 HTML 缺少组件作用域标记，未应用组件内蓝色样式。代码块同时混用终端黑色标题、三色圆点和不适合浅色界面的配色。
- **实现内容：** 将链接、行内代码、代码块及高亮的决定性明暗样式统一到 `src/style.css`，正文、列表和计划卡共用。链接默认蓝色；行内代码改为灰色底纹和等宽字。代码块采用无重阴影的灰色圆角容器、代码图标和规范语言标签，并提供复制图标反馈与逐块换行按钮。普通 Vue 代码块和 HTML 分支共用标题生成器、复用列表事件委托，不改链接解析或代码原文。
- **用户影响：** 不悬停也能识别链接；YAML/Bash 等语法颜色在明暗主题清晰。长行默认局部横向滚动，可切换换行；复制逐字保留缩进、换行和特殊字符。手机按钮至少 44px，键盘可操作，未知/无语言有明确回退。换行不保存到后端，不执行代码。
- **测试验证：** 3 个相关文件共 36 项单测、前端类型检查及构建通过；真实 TestChat 唯一标记六组桌面/手机/平板明暗检查通过，覆盖三个链接的 href/title/text、默认蓝色和跳转，五种代码块逐字复制、换行、触控尺寸、刷新和无横向溢出。首次测试请求等待过久，仅中断隔离测试回合，随后 Astra 真实回复验收完成；正式会话不受影响。
- **性能审计：** 主 JS 仅增加 72 字节（gzip 43），CSS 增加 3602 字节（gzip 768）；保留高亮懒加载与原缓存，不增加依赖、请求、计时循环或历史扫描。复制/换行只操作当前块，未触发模型或配置写入；完成态 TestChat 性能前后采样另附验收记录。未测实机软键盘或低端 GPU。
- **部署或回滚：** 先供独立 13511 验收，只更新前端，不创建备份、不重启后台、不改全局配置；13510 须用户另行明确授权升级。回退从指定提交重新生成前端并原子发布。未推送 GitHub 或发布 npm，详细操作、截图和边界见 `tests/chat-composer-rendering/fenced-code-block-copy.md`。

- **13511 发布后验收：** 前端 `c7ea54d` 已发布，33 个实际 HTTP 资源匹配；保留 Service Worker 的六组复验通过，共 18 组链接属性与蓝色检查、30 次逐字复制及换行检查，无页面错误。正式 13510 文件与验收配置哈希不变，后台进程与 5 个验收会话保留，无备份、无重启。
- **完整性能结果：** 完成态同路由 API 总量均 129 KB，高亮加载均 1 次，thread/resume 均 1 次，既有 thread/list 首页面重复 2 次未新增；首消息 996.4→532.2ms、长任务 2→3 为单次样本，不声明性能提升。聊天懒加载 JS 增加 1020 字节、聊天 CSS 减少 12506 字节，连同主入口四项资源原始体积合计减少 7812 字节、gzip 增加 359 字节。

### 正式 13510 升级全局快速模式修复

- **问题或目标：** 用户明确要求升级 13510，将已在独立 13511 验收的全局 Fast 保存修复及唯一加号入口发布到正式控制台。
- **实现内容：** 直接复用 `a351a1a` 前端产物，不重新构建不同版本；核对后续提交仅为文档、两端发布目录独立。先发布哈希资源，再原子替换 index，更新 14 个文件，33 个正式 HTTP 资源逐一与验收产物匹配。
- **用户影响：** 刷新后使用新版：「＋」是唯一快速模式入口，开关全局保存，旧读取不会覆盖新选择，模型面板不再提供速度选项。升级保留当前已保存的关闭状态，不代替用户开启。未重启服务或中断当前回合，598 个会话、配置及模型目录校验保留；13511 不变。
- **测试验证：** 复用上一批 207 项单测、六组真实配置/刷新检查和四组打包容器回归。正式 `http://127.0.0.1:13510/#/` 以 1440×900、375×812、768×1024 明暗共六组验证刷新后的资源、全局开关原值、唯一入口、模型列表及 Esc 关闭；无配置或线程写入、无发送消息、无页面错误。后端继续为 `2e17039`、Codex 0.153.4，主进程 414785 和子进程 414813 不变，前端版本以部署回执的 `a351a1a` 为准。
- **性能与部署注意：** 此次仅静态资源发布，不新增运行时代码、请求或轮询；沿用修复的性能审计，主 JS 减少 1168 字节、CSS 减少 1315 字节。回执 `output/playwright/global-fast-production-13510/deployment.json` 记录 `backupCreated:false`、`backendRestarted:false`。不创建旧文件副本；原有哈希资源留在原位供已打开页面加载。回退应从目标提交重建前端并原子发布；未推送 GitHub 或发布 npm。

### 修复全局快速模式刷新丢失，并统一到加号入口

- **问题或目标：** 在支持原生会话设置的已有聊天中，Fast 开关只改变界面；保存函数在全局配置写入前提前返回，刷新后读取旧值。模型配置中的第二个速度入口也与用户要求的全局开关重复。
- **实现内容：** 所有聊天和新聊天均通过现有 `config/batchWrite` 保存 Fast；用写入版本计数拒绝写入开始前或写入期间发起的旧配置读取，保留失败回退和提交期间防重复。配置读取完成即恢复全局速度，不再等待模型目录返回后覆盖。删除模型配置的速度行、子菜单、事件和样式，仅保留「＋」开关，并注明“所有聊天共用，刷新后保留”。
- **用户影响：** 开启或关闭后刷新仍保留，同一 WebUI 服务的其他聊天、全新浏览器读取相同设置；模型选择仍独立，橙色闪电、强度滑杆和 Ultra 动效保留。实际 Fast 参数继续按模型目录选择；不支持的模型不强制启用，不追溯改写进行中的回合或已有队列快照。其他已打开浏览器刷新后读取最新值。
- **测试验证：** 先用 5 项失败回归复现漏写，再修复；5 个相关文件共 207 项单测、全量构建和公共 CLI 的 CJS 调用通过。真实 13511 配置接口上通过六组明暗视口的全新浏览器/刷新检查、全局关闭和模拟失败回退：三次成功开关各一次批量写入，一次注入失败回退，零真实模型消息，原 Fast 设置恢复。打包镜像在 4191–4194 四个隔离端口完成缺失/损坏认证、无效认证错误刷新保留和模拟 provider 切换检查，重复错误浮层为零；当前版本仍为 Codex-only，不恢复历史 Zen 兜底。
- **性能审计：** 主 JS 716403→715235 字节、gzip 222906→222719；主 CSS 536007→534692、gzip 60409→60230。一次开关只调用一次已有批量写入，无额外启动读取、轮询、全量文件扫描或无界并发；旧读取保护仅常数时间版本比较。未测真实供应商的加速幅度、手机实机键盘或低端设备帧率。
- **部署或回滚：** 此修复先供独立 13511 验收，不授权修改正式 13510。仅发布前端静态资源，沿用同一验收 CODEX_HOME 与后端；不备份、不改权限、不推送 GitHub、不发布 npm。需要回退时从目标提交重建前端，再原子替换静态入口。截图、容器命令和详细证据见 `tests/chat-composer-rendering/composer-mode-scoping-and-fast-mode-support.md`。

- **13511 发布后复验：** 修复前端 `a351a1a` 已发布，33 个 HTTP 静态资源与构建逐一一致；六组明暗尺寸在已部署页面（常规场景保留 Service Worker）完成刷新、全新浏览器、唯一速度入口与 Esc 检查，关闭持久化及注入失败回退通过。主进程 491027、app-server 子进程 491058 和 4 个验收会话保留，原 Fast 设置已恢复；正式 13510 静态文件校验保持一致。回执记录 `backupCreated:false`、`backendRestarted:false`。

### 正式 13510 升级至最新输入区与 Ultra 动效

- **问题或目标：** 用户明确要求更新 13510；延续此前不创建备份的约定，将 13511 已验收的 `475caf8` 前端发布到正式地址。
- **实现内容：** 本次包含 `3de8d7e` 的模型选择竞态修复、`91ddd12` 的输入区与思考强度滑杆，以及 `475caf8` 的手机菜单居中、Ultra 粒子和橙色闪电。核对相对已部署 `af2aabe` 的改动仅涉及前端、测试和文档，先写入内容哈希资源，最后原子替换 index；33 个正式 HTTP 资源与验收产物逐一校验一致。
- **用户影响：** 刷新 13510 即可使用最新界面。正式主进程 414785、Codex 子进程 414813 和预览主进程 491027 均不变，保留升级前 598 个会话文件以及正式配置、认证与模型文件哈希。没有重启正在执行任务的后端。
- **测试验证：** `http://127.0.0.1:13510/#/` 的 375×812、768×1024、1440×900 明暗共 6 组通过，检查真实新资源、无蓝色焦点光圈、菜单完整行与手机居中、Ultra 粒子运动和刷新不退版。页面异常和 HTTP 错误为 0；验收仅改变浏览器中新聊天草稿选择，保存的线程偏好写入和 turn/start 均为 0。
- **性能审计：** 复用 13511 已构建并验收的相同字节，没有新构建、新运行时逻辑或额外后端请求路径；沿用上文粒子与菜单性能测量，本次未重测正式长会话性能。
- **部署或回滚：** 回执明确 `backupCreated:false`、`backendRestarted:false`，13511 静态资源保持不变。前端版本为 `475caf8`，runtime-info 的后端仍是 `2e17039`、CLI 0.153.4，不能混同。原有带哈希资源保留在原位兼容旧页面，不复制旧文件作为备份；如需回退，从指定提交重新生成前端并发布。未推送 GitHub 或发布 npm。

### 修复手机添加菜单并增加 Ultra 白点微动

- **问题或目标：** 手机「＋」菜单顶部的技能入口被旧工具栏宽度挤压，菜单向右溢出贴边；用户要求居中留边、Ultra 白点微动，以及恢复快速模式闪电的橙色。
- **实现内容：** 移除菜单内技能入口的工具栏专用类，权限与技能使用完整独立行；菜单挂载到 body，按可视视口居中并保留 16px 边距，常规手机场景锚定输入框上方 8px，过长内容内部滚动；全屏输入时菜单和子菜单保持在输入层上方。仅打开时观察布局变化，保留嵌套菜单、外部关闭和 Esc 行为。闪电恢复为 `#f97316`。
- **实现内容：** Ultra 渐变内增加固定 12 个白点，仅以 transform/opacity 做轻微漂移与明暗变化；普通档位移除粒子，页面隐藏暂停，减少动态效果使用静态白点。沿用既有拖动和保存逻辑。
- **用户影响：** 手机上方控件不再挤成小块，菜单两边对称且不贴右侧；快速模式标识恢复橙色，Ultra 有轻微动效。
- **测试验证：** 前端类型检查和构建通过；375×812、376×694、360×568 大字号、768×1024、1440×900 明暗 10 组菜单检查通过，另有明暗 2 组粒子运动、隐藏暂停、减少动态效果及无额外设置写入检查，以及两组短视口/全屏输入回归。真实服务资产逐一校验，证据记录在输入区分域测试文档。
- **性能审计：** 1.2 秒动画采样新增布局次数为 0、样式重算为 3、额外偏好写入为 0；主 JS gzip 增加 854 字节，主 CSS gzip 增加 261 字节，无新依赖或 JS 粒子帧循环。菜单监听器与观察器只在打开期间存活，没有新增请求、轮询或数据扫描；低端设备电耗未测。
- **部署或回滚：** 仅原子更新独立 13511 前端，不创建备份、不重启后端，13510 文件与两服务 PID 保持不变。未推送 GitHub 或发布 npm；如需回退，从指定提交构建前端后重新发布。

### 按桌面与手机参考图重做输入区和思考强度滑杆

- **问题或目标：** 用户要求底部布局接近参考应用，支持拖动思考强度，并去掉聊天输入框整圈蓝光。
- **实现内容：** 使用中性边框、轻阴影和紧凑工具栏；技能、会话控制及桌面听写收进「＋」，保留手机麦克风。上下文改为可点击小圆环，模型/强度入口打开连续跟手、松手吸附的滑杆；提供键盘调档、触摸取消、默认强度重置，以及按真实模型能力生成的档位。Ultra 使用静态紫色渐变。模型和速度配置在桌面菜单、手机底部面板呈现，均支持外部关闭。
- **实现内容：** 权限显示可读名称、说明和选中标记，保留原生 profile 允许列表和原有会话作用域；按需合并权限读取并丢弃切换线程后的迟到结果。通用菜单观察异步内容尺寸并重新定位；Esc 由内层先处理，避免搜索和父菜单一起关闭。移除被新菜单替代的旧手机设置模板与状态代码。
- **实现内容：** 实测发现初始偏好读取晚到时会覆盖刚选中的模型；合并服务端响应时保留读取期间被本地替换的偏好条目，模型目录返回后再读取最新选择并拒绝跨聊天迟到结果。增加延迟读取回归，浏览器同时核对保存请求的 model 和 reasoningEffort，避免仅强度正确却写入旧模型。
- **用户影响：** 聚焦输入框不出现蓝色光圈；拖动过程即时预览，松手才保存一次，刷新后保留模型/强度。原有附件、技能、语音、队列和原生控制仍有入口，不发送额外模型任务、不改变运行回合的设置语义。
- **测试验证：** 前端类型检查与构建通过，4 文件 175 项相关单测通过；1440×900、768×1024、375×812、376×640 明暗 8 组真实服务检查通过，包括鼠标/触摸拖动、取消、键盘、重置、换模型、真实偏好保存与刷新、权限异步定位和旧会话控制入口；原会话控制/输入区明暗 6 组回归也通过。用量视觉夹具与真实模型偏好接口验证分别标注于分域测试文档。
- **性能审计：** 主入口 JS gzip 增加 2458 字节，主 CSS gzip 增加 2057 字节，无新依赖；拖动设置请求为 0，松手为 1，取消为 0。浮层关闭时清理监听器和观察器，权限只按需读取并在同线程复用，无新轮询、历史扫描或请求扇出；未重测正式长会话或低端 GPU。
- **部署或回滚：** 仅发布到独立 13511，采用先资源后入口的原子更新，不创建备份、不重启后端。13510 仍为上次用户确认版本，文件哈希和两服务进程不变。未推送 GitHub 或发布 npm；需要回退时从目标提交构建前端，不依赖此次不存在的备份。详细验证及截图见 `tests/theme-layout-terminal/composer-reference-slider.md`。

### 按用户要求无备份升级正式 13510 界面

- **问题或目标：** 用户明确要求将已验收的最新 iOS 界面直接升级到 13510，并且不创建备份。
- **实现内容：** 核对 `2e17039..af2aabe` 没有后端、协议或依赖变更，采用静态资源发布：先写入 14 个内容哈希资源，再原子替换 index。正式和预览目录为独立文件，33 个正式 HTTP 资源逐一与验收产物比对一致；不重新构建或重启后端。
- **用户影响：** 刷新 13510 即可使用 iOS 界面、统一细滚动条和手机首页防遮挡修复。现有任务继续运行，保留升级前 598 个会话文件；配置、模型目录文件及服务配置哈希保持一致。
- **测试验证：** 正式地址 `http://127.0.0.1:13510/#/` 的 376×694、375×812、768×1024、1440×900 明暗共 8 组通过，验证真实资源、按钮命中、项目弹窗、无页面溢出及刷新后仍为新版；页面异常和 HTTP 错误均为 0。13510 主进程 414785、Codex 子进程 414813，以及 13511 主进程 491027 均未变化。性能复用已验收同一产物，本次没有新增请求路径、轮询或运行时逻辑，没有重新测量正式长会话性能。
- **部署或回滚：** 前端版本为 `af2aabe`；runtime-info 的后端构建仍是 `2e17039`，CLI 仍为 0.153.4，不能把后端版本当作此次前端发布版本。回执明确 `backupCreated:false`、`backendRestarted:false`。未复制旧文件或创建备份；原有带哈希静态块留在原位供已打开页面加载。13511 资源哈希不变；未推送 GitHub 或发布 npm。需要回退时从目标提交重新生成前端并发布，不依赖此次不存在的备份。

### 修复手机首页项目按钮被输入框遮挡

- **问题或目标：** 真机截图反馈首页卡片撑满内容区，项目按钮被输入框遮挡，顶部图标被压扁。376×640 复现中，按钮底部约 538px、输入框顶部 491px，重叠约 47px；顶部图标由 48×48 被压为 48×30。
- **实现内容：** 让首页内容块独立纵向滚动，输入框保持单独的固定 flex 行；内容子项不压缩，通过首尾自动外边距在空间足够时居中。手机缩减留白，短窗口任务卡切换为紧凑的图标加文字横排。
- **用户影响：** 常规手机窗口完整显示项目按钮；较矮窗口、文字放大或多行草稿时，可以在输入框上方滚动到全部控件，图标保持比例，按钮可点击。项目弹窗仍可正常打开关闭，刷新不恢复遮挡。
- **测试验证：** 前端类型检查和构建通过；376×694、376×640、360×568 放大文字、375×812、768×1024、1440×900 明暗共 12 组通过，覆盖按钮真实命中、多行草稿、窗口变矮、弹窗及刷新。修复仅 CSS，不增加 API、监听器或数据遍历，性能影响限于已有首页的布局与原生滚动。
- **部署或回滚：** 仅更新独立 13511 静态资源，进程 491027 不变；13510 主进程和正式目录文件校验保持一致。保留独立预览旧 index 供回退，不重启后端、不推送或发布 npm。

### iOS 磨砂界面与统一细滚动条

- **问题或目标：** 用户改选 iOS 风格，并要求消除带上下箭头的粗滚动条；保留聊天对齐、外部关闭和未提交草稿，同时避免再次影响正式控制台。
- **实现内容：** 集中更新珍珠白/石墨色主题、磨砂导航与浮层、蓝色主操作、圆角分组和图标底色；会话控制使用分段选中背景、设置值列表与可展开说明。手机顶部拖动条支持跟手、取消回弹、上拉展开、下拉关闭，弹簧可重定向且有帧间隔上限；键盘操作与减少动态效果直接响应。滚动条透明底槽、无箭头、细圆角滑块，修复悬停时标准属性覆盖 WebKit 样式导致宽度突变的问题；聊天列表测量真实滚动条占宽，保持输入框对齐。
- **用户影响：** 手机与电脑采用同套材质、颜色和功能分组，消息阅读背景保持稳定；目标草稿在关闭后保留，队列与附件数据沿用原逻辑，面板内滚动不触发拖动关闭。
- **测试验证：** 全量构建通过，3 个相关单测文件 18 项通过；六组明暗视口回归、四组历史/队列夹具、触摸手势/滚动/减少动态效果验收完成。额外发现并修正构建后仅剩 WebKit 模糊声明的顺序问题，已核对浏览器实际 backdrop-filter 为 blur(18px)。最终独立服务验收与性能记录见分域用例和后续记录；真机键盘、Safari/Firefox 与低端 GPU 尚未测试。
- **部署或回滚：** 创建独立 `ios-preview-20260907` 发布目录，dist 与 dist-cli 为非硬链接副本，仅复用依赖；正式资源目录保持原状。13511 切换需先确认会话空闲并保留同一验收 CODEX_HOME。回滚只恢复 13511 的服务 drop-in，禁止改写共享正式 dist；未推送 GitHub 或发布 npm。

- **最终验收与部署：** 13511 已切换至独立 `ios-preview-20260907`，运行构建 `fcb39a3`（dirty=false）、CLI 0.153.4；保留原验收 HOME、配置哈希和 4 个会话。13510 主进程及已记录正式发布资源哈希在测试后均未变化。真实服务六组明暗尺寸与触摸/刷新/滚动/减少动态效果检查通过；API 119.6→118.9 KB，thread/resume 均 1 次，既有首页面列表请求均 2 次。首条消息 427.3→449.2ms、长任务均 1 次为单次采样，不声称性能提升。证据和未测边界已写入分域验收文档。

### 细滚动条与侧栏调宽的命中区域修复

- **问题或目标：** 拖动验收发现侧栏调宽按钮的透明伪元素向左扩展 8px，恰好覆盖整条细滚动条，导致滑块看得到但无法被鼠标拖动。
- **实现内容：** 将调宽命中区域改为仅向主内容区扩展 12px；自动验收检查滚动条中心命中的元素、真实滑块拖动与侧栏调宽两条路径，补充有窗口 Chromium 截图。
- **用户影响：** 滑块可直接拖动，侧栏仍可拖宽；滚轮、键盘、触摸滚动行为保留，没有新增滚动监听或布局计算。
- **测试验证：** 当前构建通过；有窗口 Chromium 明暗两组已验证滑块拖动、调宽和无双重滚动条，独立服务的完整触摸、刷新、滚动、减少动态效果复验也已通过，未发生页面异常。构建日志、真实服务测试和性能审计记录补齐于 iOS 分域文档。
- **部署或回滚：** 仅原子更新独立 13511 的前端静态资源，不重启服务；正式目录逐文件校验保持一致。回退恢复独立预览的旧 index，不修改正式目录。

### ChatGPT 桌面风格第一版与聊天布局统一

- **问题或目标：** 原有侧栏和聊天区视觉不统一，等待状态绕过聊天列导致贴左错位，原生控制常驻大表单挤压聊天空间；以用户提供的六张桌面截图作为本次基准。
- **实现内容：** 集中替换明暗主题语义颜色，采用中性灰侧栏、实色主界面、细边框、轻阴影及圆角输入框；统一正文、工具记录、等待状态、队列和输入框的左右边界。新增侧栏新聊天入口与首页四张任务卡，任务卡只填草稿。将原生设置拆为浮层中的运行与权限、目标、队列分组，独立保留可见队列；电脑入口位于输入框工具栏，手机入口位于「＋」，目标和扩展也可从「＋」直达。
- **用户影响：** 点击面板外部或按 Esc 可关闭，内部与子菜单操作不会误关，关闭保留未提交目标草稿；权限/模型/队列实际作用域保持原逻辑。手机沿用相同视觉、使用底部面板，桌面使用入口上方浮层。
- **测试验证：** 全量构建、公共 CLI 帮助命令、2 个相关单测文件 15 项通过；1440×1000、768×1024、375×812 明暗共六组浏览器验收，等待/命令对齐与队列附件保留四组夹具验收通过。增加可复用浏览器脚本与分域手工用例。性能采样 thread/resume 均 1 次，API 124.0 → 119.6 KB；既有 thread/list 首页面重复仍存在，单次时延变化不作为统计提升结论。真实手机软键盘与低端 GPU 未测。
- **部署与回退更正（10:39）：** 原先以为仅预览到 13511，但 13510 与 13511 实际共同加载 `releases/chat-fixes-20260906/dist`，静态资源替换同时影响了正式界面。收到用户反馈后已原子恢复改动前 index，两端 HTTP 返回及旧 JS/CSS 校验均与旧资源一致。两个服务均未重启，13510 的 598 个会话文件清单保留。当前两端均为旧 UI，新 UI 仅保留在功能分支；后续预览必须先隔离整个发布目录并核对两个启动脚本实际 import 路径。未推送 GitHub 或发布 npm。

## 2026-09-06

### 本次 `dev` 推送范围

本次将 `origin/dev` 从 `69750e8` 快进，发布以下 13 个已有本地提交及本次中文日志汇总，不重写历史、不合并 `main`、不发布 npm 包。2026-09-05 条目保留各批次验收当时的状态；这些批次均纳入本次推送，当前正式部署情况以本日记录为准。

| 提交 | 问题或目标、实现内容与用户影响 |
| --- | --- |
| `a58ce44` | 适配 Codex 0.153.4：展示实际运行身份和模型能力来源，接入原生请求关闭、非阻塞提问及新运行提示；模型失败保留原错误，不自动换模型或重发。 |
| `cccece1` | 补齐首批隔离打包、浏览器与 Docker 验收及分域手工用例，明确协议可用与真实运行的边界，不恢复旧 provider 自动兜底。 |
| `d2242b1` | 排除 tsup 自身临时配置造成的脏构建误报，仍检测真实未提交源码，不增加运行时开销。 |
| `9281add` | 接入插话、设置、Goal、权限与队列的原生网关；合并并发 Schema 发现、限制分页，对不确定提交先对账而非重发。 |
| `b3c5c57` | 持久化并隔离原生与兼容队列归属，串行修改并阻止双重领取；同步 CLI 确认的会话设置，不迁移旧队列消息。 |
| `5eba11f` | 增加原生会话控制界面，分清当前回合与后续设置，支持 Goal、权限和队列操作；插话失败保留草稿和原活动回合。 |
| `0436c7d` | 补充第二批真实协议、六组 UI、打包容器与性能证据；按用户约定明确升级不再备份，复用已验收产物与依赖。 |
| `a3333d1` | 接入脱敏能力读取、独占实时会话桥接和子线程过滤；SDP/音频不进入重放缓存，租约、分页与清理重试保持有界。 |
| `8069dfb` | 增加懒加载原生扩展界面，按实际能力开放语音、远控、子任务及稳定开关；插件开关受限时只读，不静默启用实验功能。 |
| `633ad9d` | 记录第三批真实运行、夹具、构建、性能和无备份验收部署的区别，保留未验证的供应商语音、远端配对和上下文引擎边界。 |
| `f31bc4d` | 分离手机附件预览与删除按钮，保留独立 44×44 触控区域及暗色样式，避免点大图时误删附件。 |
| `34738c5` | 对齐历史、实时及乐观用户消息身份和时间线，修复气泡双显与插话错序；真正重复发送仍保留，不自动重发不确定请求。 |
| `2e17039` | 将附属清理移出核心删除等待，增加进行中与失败反馈；同步删除状态、失效迟到读取，删除当前聊天后进入新建聊天。 |

### 五项已复现聊天问题修复

#### 问题或目标

- 历史临时 ID 与实时规范 ID 不一致，同一条用户消息可能出现两次。
- 手机附件删除热区覆盖缩略图，想预览大图却容易误删。
- 多轮聊天中，已发送的插话被拼到旧回复之前，表现为消息“被吞”，刷新后才在正确位置出现；后台实际已接收。
- 删除确认界面等待附属数据清理，反馈迟缓，用户难以判断是否正在删除。
- 删除后旧路由、选中状态及迟到读取相互竞争，重新加载已删聊天并显示 RPC 错误。

#### 实现内容

- 用户消息在同一回合内一对一对齐临时/规范身份，指纹包含附件与技能；保留稳定渲染键，统一历史与实时顺序，原生用户通知即时入列并合并同步。
- 已知历史 ID 优先匹配，避免相同文字的新消息被旧历史吞并；不确定插话保留乐观消息且不自动重发。
- 附件预览和删除改为相邻独立控件，共享暗色主题规则，不修改上传协议。
- 核心删除确认后立即返回，附属清理由有界后台队列处理：最多两个聊天并行、每次四项清理、最多三次尝试，服务退出取消尚未执行的重试。
- 服务端广播删除确认并对已知删除线程读取返回 410；前端去重删除请求、失效旧异步响应和路由回流，跨标签页同步。仅在当前聊天被删时进入新建聊天，不抢走用户已切换的其他聊天。

#### 用户影响

发送或插话后无需刷新即可在正确位置看到一条用户气泡，历史接管不会额外复制，真实相同内容的多次发送仍各自保留。点缩略图只预览，点旁边删除键才移除附件。删除中有明确反馈，核心失败可以重试；成功后不再等待附属清理，也不再停留在已删除聊天的报错页。

长任务持续停留后刷新不及时的问题仍未确认复现，本次没有宣称修复，也未通过周期性全量刷新或隐藏错误掩盖它。

#### 测试验证

- 本次推送前重新执行 `pnpm run test:unit`：68 个测试文件、654 项测试全部通过；`pnpm exec vue-tsc --noEmit` 与 `git diff --check` 通过。
- 已有功能验收的 `pnpm run build` 完成前端、CLI 和类型检查；本次复用既有产物执行 CJS 冒烟：`node -e "const assert=require('node:assert/strict');const result=require('node:child_process').spawnSync(process.execPath,['dist-cli/index.js','--help'],{encoding:'utf8',timeout:15000});assert.equal(result.status,0);assert.match(result.stdout,/--port/);"`，退出 0。公开 CLI 仍为 ESM，没有增加 CJS 导出或运行依赖。
- 已有 13511 发布产物验收覆盖 375×812、768×1024、1280×900 明暗六组附件实际点击；真实插话在点击、确认、刷新和完成后均只有一个用户行，位置正确，一次发送只有一次 steer 请求。
- 慢删除、核心失败重试、双标签页同步及旧地址返回通过；核心响应后弹窗关闭实测为 433 / 370 / 53 / 55ms，不承诺所有设备固定耗时。确认删除后旧线程新增读取为 0，客户端附属清理 DELETE 请求为 0。
- 已有打包容器 4191–4194 的无认证、畸形认证、无效认证及本地假 provider 切换四场景通过；错误刷新后仍保留，重复 live overlay 为 0，测试容器与临时监听已清理。
- 浏览器证据来自 Linux Chromium 移动视口，不等同于 Android 真机网络与触摸验收。本次推送只重跑单元、类型与入口检查，不重新执行浏览器、容器或真实模型任务。
- 手工回归入口：`tests/chat-composer-rendering/attachment-preview-delete-targets.md`、`tests/chat-composer-rendering/live-user-message-reconciliation.md`、`tests/thread-loading-state/active-thread-switches-after-delete.md`；前三批详见 `documentation/codex-0.153.4-phase1.md`、`documentation/codex-0.153.4-phase2.md`、`documentation/codex-0.153.4-phase3.md`。

#### 性能审计

- 功能验收时对 1000 条、约 1MB 文本的消息身份对齐测量 100 次：P50 2.44ms、P95 3.94ms、最大 19.05ms；弱引用指纹缓存不额外持有已淘汰聊天对象。
- 已有线程浏览器报告 API 总量 168.7KB，`thread/resume` 一次、重复历史分页为 0，最慢 `provider-models` 约 295.5ms。仍有 `threadListFirstPage=2` 既有告警和构建大 chunk 提示，本次没有声称解决。
- 删除后的迟到响应被失效，后台清理有并发、排队及重试上限；消息同步合并请求，不增加逐 token 请求、定时全量历史刷新或新轮询。上述测量来自修复验收，本次日志提交没有运行时行为变化。

#### 部署或回滚注意事项

- 用户单独要求的 13510 升级已于 2026-09-06 01:49:51（Asia/Shanghai）完成：直接切换到已验收 `2e17039` 产物，构建 `dirty=false`，保留升级前 126 个会话，CLI 仍为 0.153.4。
- 独立部署回执确认 systemd、端口归属、HTTP、RPC、CLI 进程归属和局域网密码登录通过；正式配置、模型目录及 13511 进程保持不变。没有备份、重建、修改 WS 或迁移正式历史。
- 本次 GitHub 推送只发布源码和文档，不再次重启 13510/13511，不修改正式配置，也不另行升级 Codex CLI。WebUI 包版本仍为 0.1.87；本次新增日志提交不改变已部署业务代码。
- 回退复用已有 WebUI 产物，不新建备份；永久删除的聊天无法通过回退代码恢复。退到不识别原生队列归属的旧版本前，须清空原生队列并切回兼容归属，不能直接删除归属文件。

## 2026-09-05

### 第三批 Codex 原生扩展（本地完成，尚未推送）

#### 问题或目标

补齐原生实时语音、远控、子任务历史、插件/实验生命周期和上下文状态入口；区分协议接受、配置加载和真实执行，避免照搬通用文档或以 HTTP 200 作为成功。

#### 实现内容

- `a3333d1`：新增能力读取与独占实时桥接、按线程串行租约/停止清理、非重放 SDP/音频通知、ancestorThreadId Schema 检测、有界子线程网关及协议回归。
- `8069dfb`：增加五页签懒加载扩展界面，WebRTC V1/audio 麦克风/播放/文本/转写和退出清理，ephemeral 远控确认/配对/客户端管理，活动卡子线程链接与原生历史恢复、插件禁用原因/版本和上下文证据。
- `8069dfb`：真实 RPC 揭示 apps/plugins 运行时写入被 0.153.4 忽略，改为只读；仅开放精确安装版本支持的五个稳定键，校验托管锁、ACK 和状态回读。修正 client 单数远控方法，按线程读取实际 feature 与 provider，保留全局默认/当前线程的范围标识。
- `8069dfb`：修复无 randomUUID 的 HTTP LAN 页面、BFCache 返回的连接状态、确定不支持 realtime 后残留假租约、桥接关闭后仍启动排队连接；增加真实 native 和六主题尺寸浏览器脚本、分域手工用例、缓存 npm 的独立打包前缀。
- 验收资料提交：记录真实协议限制、构建与性能数字、四场景 Docker 验收、13511 无备份部署和不覆盖正式配置并行变化的约束。

#### 用户影响

会话底部或首页设置可以打开“原生扩展”。语音需要用户授权、安全浏览器上下文、显式实验配置及提供商支持，不默认使用 Astra、不自动开麦/重连；远控不是网页访问，必须用户确认且只临时启用。插件已安装不等于可调用，notLoaded 子线程不标为完成，普通压缩不被当成新上下文引擎生效。新开关不写全局 TOML，已有正式会话、队列和模型设置不迁移。

#### 测试验证与性能审计

- 66 文件、640 用例通过；类型检查、Vite/tsup 和 CJS 动态导入公开 CLI 帮助退出 0。原有大 chunk 告警仍在，没有增加依赖。
- 真实 0.153.4 方法目录、135 项 feature、9 个 V1 声音、线程范围/ancestor 读取、tool_suggest 临时切换回读/恢复、禁用 realtime 的明确拒绝及租约释放通过；两个隔离 HOME 的配置均保持不变。
- Astra low 只读 TestChat 链接 href/title/text 通过；1280×900、375×812、768×1024 明暗六组真实 UI 断言通过，页面异常 0。音频媒体交换和远端配对采用夹具，未冒充真实供应商语音、真实远端设备或新上下文引擎执行已验收。
- 打包 Docker 4191–4194 四场景通过：无 auth、畸形 auth、假 401 明暗刷新持久错误且重复 overlay=0、原生 provider Astra→Luna；入口是新 `/opt/phase3` 安装，复用已有 CLI/缓存，164 包安装约 22 秒，测试容器清理。
- 首页/线程 API 为 182.0/125.8 KiB，线程首次真实消息 773.7 ms，resume 1 次，无重复历史页；关闭面板时扩展/实时/远控新请求 0。保留已有首屏 thread/list=2 和首页 config/read=2，不声称 P95 改善。长任务各 1 个、最大 137/138 ms。
- 主入口 694.16 kB，扩展/实时/远控异步块 16.90/9.74/7.23 kB。能力 32 范围/15 秒，单元 20 并发共用 4 类 RPC；真实缓存读取 20 次约 276 ms、单份 17639 字节。子线程最多 200 条，无逐项 resume；新事件复用原 WebSocket，转写及清理重试有界。

#### 部署或回滚注意事项

本批仅更新 13511，新建的是本批构建产物目录，不是备份；复用已有隔离 HOME、固定 CLI 与依赖符号链接，不复制正式历史或全量依赖。13510 已使用 phase2 目录，不能覆盖该目录，也不重启正式服务。原生语音/上下文/远控不静默启用，supports_websockets=false 保留。回退前显式停止自建语音/远控并等待原生队列清空，使用已有产物，不新建备份。

核验发现正式 config.toml 在 22:33:42 有并行变化，来源未确认；本批没有正式配置写入，不覆盖/还原，也不宣称其字节全程不变。正式 PID 未变，WS 仍关闭。完整说明见 `documentation/codex-0.153.4-phase3.md`。

### 第二批后的独立 13510 升级

- 问题或目标：用户明确要求先升级 13510，保留所有原会话，不再生成备份。
- 实现内容：上一轮独立升级等待活动回合结束后，直接复用已验收的 phase2 `0436c7d` 产物与现有依赖，13510/13511 使用不同 runner 和 CODEX_HOME。
- 用户影响：13510 的 127 个原会话保留，CLI 为 0.153.4，页面密码登录方式不变；没有将隔离测试历史混入正式 HOME。
- 测试验证：独立升级记录 phase=complete，正式 PID 366146、CLI PID 366174、构建 dirty=false；CLI 路径、HTTP/LAN 登录、线程集合、当时的配置和模型目录均核对通过，13511 未变。
- 部署或回滚注意事项：此为第三批之前已完成的用户指定升级，并非本批再次升级正式服务；未来升级继续复用已验证产物，不做备份、不覆盖共享目录。

### 第二批 Codex 原生会话控制（本地完成，尚未推送）

#### 问题或目标

插话应进入原活动回合，而非继续套用 turn/start；运行设置、会话设置与全局默认需要明确分层。原生 Goal、权限与队列应按实际 CLI 能力接入，不能与已有计划卡、兼容队列混用或在失败时丢弃草稿。

#### 实现内容

- `9281add`：抽离并合并实验 Schema 方法/通知目录生成，按 CLI 身份缓存；增加精确 turn/steer 输入构建、原生设置/Goal/权限/队列类型和网关、分页上限、提交确认丢失后的对账而非重发。
- `b3c5c57`：增加独立原生队列归属文件、每线程 mutation 串行与双队列清空/空闲切换校验；阻止旧队列写入和领取原生线程；跟踪 native queue/start 运行状态，缓存并通知 CLI 实际会话设置。
- `5eba11f`：加入原生会话控制面板，区分当前与后续设置、以实际 step_model_switching 开关门控；Goal CRUD/状态/预算/用量、allowed-only 权限、原生队列编辑排序和开始；确认接收才清理相同草稿，插话错误明确呈现且保留原回合，已有会话 Fast 不写全局默认。
- 验收资料提交：补充真实 Astra/RPC 与六种主题尺寸 CJS 脚本，复用已安装 CLI 的打包容器矩阵，增加分域用例、性能证据与部署/回退说明；旧首批说明中的“今后备份”要求按用户新约定改为不备份、复用已验证产物。

#### 用户影响

已有会话可展开“原生控制”。插话不再重开回合；Goal 与 update_plan 保持独立；权限只作用于该线程后续回合。原生队列需主动启用且两种队列都为空，旧消息不会自动迁移或丢失逐条模型/Plan 设置。缺少实验开关时运行中设置明确不可用，不自动修改正式配置。CLI 会自动领取原生队列，手动开始竞争失败后只对账，不重发。

#### 测试验证与性能审计

- 63 文件、620 用例通过，类型检查、Vite/tsup 构建和 CJS 动态导入公开 CLI 帮助退出 0。
- 真实 Astra low 同回合插话进入最终文本；Goal CRUD/预算保留、会话设置、只读权限、运行设置 applied、队列增删改排序和自动续跑均验证。手动 start 实测输给自动领取，未声称此次手动调用成功。
- 1280×900、375×812、768×1024 明暗主题共六组通过；实际控件、文件 href/title/text、拒绝保稿、权限禁止项、Goal 刷新及双标签通知、队列附件保留都检查，页面异常为 0。
- 打包 Docker 在 4191–4194 的无 auth、畸形 auth、假凭据 401、原生 provider 切换四组通过；401 明暗刷新后仍是单条错误、零重复 live overlay。复用既有 CLI 镜像，在独立安装前缀使用 npm 缓存，避开全局旧包 rename/copy-up 的慢路径；未复制或备份正式依赖和数据。
- Schema 20 并发生成 1 次；通知 120 ms 合并，原生列表 20 页/2000 项/8 MiB、有界缓存与懒加载，无新增 WebSocket 或原生队列轮询。
- 首页 API 182.0 KiB；线程 API 126.3 KiB、首次真实消息 1288.7 ms、resume 1 次、重复历史页 0。开关发现新增 2 页/19.9 KiB；保留原有 thread/list 首屏 2 次告警及首页 config/read 2 次，不伪称零开销或 P95 提升。主入口约 693.4 kB，原有大 chunk 告警仍在。

#### 部署或回滚注意事项

本批只更新 13511 验收实例，保留其独立 HOME 和已有验收会话，复用兼容依赖及固定 CLI 0.153.4。13510 不升级、不重启，正式配置哈希与 `supports_websockets=false` 保持不变；不新建备份、不复制正式历史、不推送或发布 npm。开发中设置开关只在验收 HOME 启用。回退更旧且不识别原生归属的版本前必须先等待原生队列清空并切回兼容归属，不能直接删除归属记录。

### 首批 Codex 0.153.4 兼容升级（本地完成，尚未推送）

#### 问题或目标

日常 `dev` 需要区分实际常驻 CLI 与全局安装版本，正确发现 Astra 的完整能力，并处理新协议的原生请求关闭、非阻塞澄清和运行提示。原来“模型不支持就自动换模型、回滚重试”的路径会改变用户选择，需要取消。正式实例和升级验收必须隔离，不能共用正式数据库试升级。

#### 实现内容

- `a58ce44`：增加 WebUI 构建身份与实际 app-server 握手版本，设置中懒加载运行信息；忽略共享依赖符号链接，避免错误标记脏构建。
- `a58ce44`：按原生 model/list → 同版本 bundled → 明确未知的顺序读取能力，保留 Astra 的六档推理与 Fast 的真实 `priority` 值，显示来源说明，不用硬编码伪造模型能力。
- `a58ce44`：增加默认仅预览的隔离配置准备脚本，补充完整缺失模型 profile，保留默认模型、上下文窗口和压缩阈值；不复制正式会话、SQLite、队列，凭据配置使用严格文件权限。
- `a58ce44`：统一数字/字符串请求 ID，处理原生 `serverRequest/resolved`、generation、线程归属、重复请求及过期快照，原生关闭不虚构用户答案。
- `a58ce44`：非阻塞提问保留独立回答入口但不吞普通消息；分开呈现安全缓冲、账号验证、认证恢复和服务端重路由；增加新消息的有界、折叠、明暗主题渲染。
- `a58ce44`：移除自动换模型、回滚和重发，保留真实失败回合与原错误；顺带移除该重试路径不再需要的输入副本。
- 本轮验收资料提交：加入六种主题/尺寸的 Playwright CJS 回归脚本、固定 CLI 的打包 Dockerfile、假凭据 401/provider 切换测试、分域手工用例及部署/回滚说明；根 tests.md 仍仅作为索引。
- 最终部署核验修正：tsup 在执行构建配置时会临时生成 `tsup.config.bundled_*.mjs/cjs`，使干净提交被误判为脏构建。仅忽略这些构建临时文件，不忽略未提交业务源码；重新从干净提交构建并核对运行实例的提交与 dirty 标识。没有增加运行时请求或依赖。

#### 用户影响

Astra 可以从隔离目录加载，模型选择不会被初始异步刷新覆盖；ultra 与 Fast 的参数值可正确传递，但服务商是否实际开放、提速或如何计费仍需真实运行证据。非阻塞问题不再被显示成必须回答才能继续。出现模型拒绝或鉴权失败时，不再自动替用户改模型。原生 steering、queue、Goals、新权限完整界面及工具图片输出完整呈现不属于本批。

#### 测试验证与性能审计

- 59 个测试文件、591 个用例通过，vue-tsc、Vite、tsup 构建及公开 ESM CLI 的 CJS 动态导入帮助冒烟通过。
- 隔离 TestChat 的 Astra low 实际完成 README 工具读取和最终标记，文件链接 `hrefOk/titleOk/textOk` 均为 true；不把选项存在误报成 ultra/Fast 已经实测生效。
- Playwright 覆盖 1280×900、375×812、768×1024 的明暗主题，验证新消息、非阻塞输入、原生关闭、刷新不复活、验证提示恢复及设置单次请求，页面异常为 0。
- 打包 Docker 在 4191–4194 覆盖无 auth、畸形 auth、假凭据 401 和原生 custom provider 切换；401 刷新后保留一个错误、零重复 live overlay，无自动回滚/换模型。
- 性能报告：首页 API 156.3 KiB；线程 API 92.6 KiB、首次真实消息约 656 ms，无重复历史页。原有终态恢复路径仍会发第二次 thread/list，记录为后续优化，不宣称零重复或 P95 提升。
- 元数据读取 100 并发合并为 2 次子进程；新提示复用现有请求、有界存储，工具输出保留事件/历史/UI 三级预算，无新增运行依赖。

#### 部署或回滚注意事项

正式 13510 不重启、不部署，原 CODEX_HOME 配置与模型目录哈希保持不变。13511 使用独立发布目录、完整固定 0.153.4 CLI 和独立 HOME，原 unit/runner/产物有备份。回退测试 WebUI 时继续保留隔离 HOME，不直接恢复会重新共享正式 HOME 的旧 runner。正式升级另行确认维护窗口；没有发布 npm 包或推送远端。

## 2026-08-26

### 本次 `dev` 推送范围

本次推送将 `origin/dev` 从 `66b7c8f` 快进到计划能力发布提交，包含以下尚未发布的功能与修复：

- `38aafee`：增加活动计划服务端快照。
- `0b17cc2`：恢复刷新中的活动计划。
- `fba5c87`：完善计划卡生命周期折叠。
- `e40c22c`：补充计划恢复与折叠验收文档。
- `7a8239c`：限制计划快照握手总负载。
- `0e02f41`：记录活动计划恢复功能变更。
- `529a743`：修复完成计划摘要闪现后消失。
- `adf2d78`：实现规划模式命令与常驻状态。
- `df0824b`：持久保留规划澄清问答摘要。
- `972d9af`：同步线程规划模式并增加单次澄清入口。
- `b12bcf3`：避免空浏览器抢占规划模式迁移。
- `81a23d7`：持久化终态计划摘要。
- `c2e02ad`：按回合归位计划摘要。
- `8eaf67c`：收紧计划摘要性能边界与验收说明。

### 活动计划刷新恢复与生命周期管理

#### 问题或目标

- `plan.live` 过去只存在于浏览器内存；刷新时计划卡会消失，历史加载和实时恢复交接时还可能短暂重复。
- 完成、失败、中断和未完成计划缺少稳定的折叠边界，长任务会持续占位或在用户尚未看清时消失。

#### 实现内容

- 服务端新增有界 `ActivePlanSnapshotStore`，从计划通知中维护按线程和回合隔离的活动快照，并在通知连接的 `ready` 事件中恢复。
- 前端提前建立通知连接，按 `threadId + turnId + messageId` 幂等接管计划；历史到达后按回合合并，避免同一计划出现两张卡。
- 计划卡增加 `live`、`completed`、`failed`、`interrupted` 和 `incomplete` 生命周期；运行中展开，完成后折叠，异常状态在当前轮保留，下一轮再收起。
- 快照同时受数量、单项大小和 512 KiB 总序列化预算约束，优先保留最近活动数据。

#### 用户影响

- 长对话刷新后可以先恢复正在运行的计划，不必等待完整线程历史。
- 完成计划稳定保留折叠摘要；失败或未完成计划不会立即消失，用户发送“继续”或新任务后仍以新回合为边界独立展示。
- 用户手动展开或收起的状态会在刷新后保持。

#### 测试验证

- 覆盖服务端快照、通知握手、历史接管、生命周期、容量与字节预算、重复通知和终态恢复测试。
- 13511 实测活动快照可早于延迟历史出现，恢复后仍只有一张卡；单次刷新只建立一条通知连接，没有新增计划轮询。

#### 部署或回滚注意事项

- 需要同步部署前端和 CLI 服务并重启 WebUI，旧服务端不提供快照时前端仍可退回历史加载流程。
- 快照是进程内有界数据，回滚无需迁移或清理数据库。

### 线程规划模式与结构化澄清

#### 问题或目标

- Composer 的规划模式开关和 `/plan` 过去没有完整接入线程协作模式，中途切换后可能仍按普通模式发送，无法稳定触发结构化提问 UI。
- `request_user_input` 的问题与答案只存在于当前页面，刷新或换浏览器后缺少可追溯记录。

#### 实现内容

- 将规划模式持久化为线程级协作偏好，使开关与 `/plan` 使用同一状态，并在发送时显式传递 `collaborationMode: "plan"`。
- 首次进入规划模式时允许一次结构化澄清，后续按 Codex 的实际工具调用决定是否继续提问或输出计划。
- 新增服务端澄清摘要存储及 API，按线程保存有限数量的问答记录，跨浏览器同步并避免明文保存敏感答案。
- 浏览器旧状态迁移仅在确实存在遗留数据时执行；空浏览器不会写入初始化标记，也不会抢占已有服务端状态。

#### 用户影响

- 在已有对话中开启规划模式后，下一次发送会真正进入 Plan 模式；Codex 需要关键选择时会显示结构化提问 UI。
- 规划模式和澄清摘要可跨刷新、跨浏览器保持，普通 `/plan` 命令与界面开关不再表现为两套逻辑。

#### 测试验证

- 覆盖线程偏好、首次澄清、空迁移、跨浏览器同步、问答容量和敏感字段处理。
- 相关版本通过 51 个测试文件、551 项测试以及桌面、手机、平板的明暗主题验收。

#### 部署或回滚注意事项

- 会新增 CODEX_HOME 下的线程协作偏好与澄清摘要状态文件；写入采用有界、串行和原子替换策略。
- 回滚代码时可以保留状态文件，旧版本会忽略这些文件，之后重新部署仍可继续使用。

### 终态计划摘要持久化与按回合归位

#### 问题或目标

- Codex 不保证把 `update_plan` 作为原生历史 item 保存；仅依赖 `plan.live` 会让完成摘要在刷新后消失。
- 旧恢复逻辑在新计划结束时重新追加历史计划，造成多张旧摘要突然集中出现在聊天底部。

#### 实现内容

- 新增 `plan.summary` 终态模型和服务端 `plan-summary-history` 存储/API，按 `threadId + turnId` 保存完成、失败、中断或未完成摘要。
- 加载历史时把摘要插回所属回合，而不是追加到会话末尾；原生 `item.type = plan` 始终优先并接管生命周期元数据。
- 每个线程的实时卡最多一张；历史摘要限制为每线程 128 条、全局 2048 条、聚合结构化文本 32 KiB，并使用确定性裁剪。
- 终态摘要不再显示不合适的 `Implement plan` 操作，手动展开状态继续按回合持久化。

#### 用户影响

- 完成卡硬刷新后仍会出现，并固定在对应用户消息和最终回复之间。
- 新计划完成时只新增当前计划卡，不会让 A/B/C 等旧计划一起在页面底部重新出现。
- 折叠卡保持紧凑，展开后显示完整步骤；刷新后仍保持用户上次选择的展开状态。

#### 测试验证

- 完整单元测试通过：53 个测试文件、563 项测试全部成功；`pnpm run build` 和 `node dist-cli/index.js --help` 冒烟通过。
- 13511 使用现有可视化 Chrome 验收 A/B/C/D 四个真实计划回合，两次硬刷新后均按原回合恢复，B 卡手动展开状态保持。
- 每次硬刷新只有一次 `GET /codex-api/plan-summary-history` 且返回 200；1022×867 下无横向溢出，浅色和深色主题均通过，当前页面控制台 0 错误。
- 构建输出中主前端包为 659.24 kB，`ThreadConversation` 分块为 94.96 kB；有界存储与单次读取避免无界扇出和重复请求。

#### 部署或回滚注意事项

- 部署需同时更新 `dist` 与 `dist-cli` 并重启 WebUI；状态文件为 `linux-codex-webui-plan-summary-history.json`，权限限制为仅当前用户可读写。
- 回滚无需删除摘要文件；旧版本不会读取它，重新升级后仍可恢复已有终态摘要。

## 2026-08-24

### 修复完成计划折叠摘要闪现后消失

#### 问题或目标

- `turn/completed` 会先把计划卡转为折叠终态，但紧随其后的历史刷新和运行态对账又清除了唯一的 `plan.live`，造成摘要只闪现约 1–2 秒。
- Codex 当前不保证将 `update_plan` 持久化为线程 `plan` item，因此不能假定历史已经有可接管的卡片副本。

#### 实现内容

- 终态清理默认保留实时计划；只有现有历史去重逻辑确认同一 `turnId` 的持久化计划存在时，才移除实时副本。
- 每个线程最多保留 64 张实时计划，溢出时淘汰最旧记录，防止长会话内存无界增长。

#### 用户影响

- 计划完成后会稳定保留一行折叠摘要，后台历史刷新和运行态轮询不再使它消失，用户仍可点击展开详情。
- 服务端终态快照过期且原生历史未包含 `update_plan` item 时，硬刷新仍可能缺少该卡；本修复解决的是当前页面内的错误清理。

#### 测试验证

- 新增“后续历史不含 `update_plan` item”的回归用例，确认完成卡在强制历史刷新后仍保留且 lifecycle 为 `completed`。
- 新增 65 个连续计划的容量用例，确认仅保留最新 64 张。

#### 部署或回滚注意事项

- 仅改变前端内存清理策略，不新增端口、请求、持久化文件或数据迁移。
- 回滚后将恢复终态对账时清除实时计划的旧行为。

## 2026-08-20

### 增加活动计划服务端快照（`eeb5f12`）

#### 问题或目标

- 页面刷新会丢失浏览器内存中的 `plan.live`，只能等待完整历史返回后再看到计划。

#### 实现内容

- 新增有界的 `ActivePlanSnapshotStore`，消费 `turn/plan/updated`、`item/plan/delta`、`turn/started` 和 `turn/completed`，保留结构化步骤、修订号与生命周期。
- SSE 与 WebSocket 的 `ready` 通知统一携带 `activePlans`；完成、失败或中断快照保留 30 秒便于刷新交接。

#### 用户影响

- 后续前端接入后，硬刷新可从已有通知连接恢复活动计划，不需新增 GET 或轮询接口。

#### 测试验证

- 覆盖结构化更新、文本 delta、终态过期、新轮次接管、非法通知、容量上限和 SSE `ready` 集成。

#### 部署或回滚注意事项

- 快照仅存于 WebUI 进程内存；重启服务后由线程历史接管，无数据迁移。

### 恢复刷新中的活动计划（`ca006c4`）

#### 问题或目标

- 通知连接原本在历史与路由初始化之后才建立，长线程会让快照恢复继续等待，实时卡与历史卡也可能短暂重复。

#### 实现内容

- 将通知流提前到选中路由初始化后建立，但将运行状态轮询保留在 `refreshAll` 与路由同步完成之后。
- 按 `threadId + turnId + messageId` 幂等恢复 `plan.live`，历史到达后按 `turnId` 合并终态元数据并接管，同一轮只显示一张卡。

#### 用户影响

- 长对话刷新时，活动计划可先于历史消息出现；新一轮开始不会误删旧计划。

#### 测试验证

- 单元测试覆盖历史 Promise 未完成时即恢复、重复 `ready` 幂等、轮询延后启动、历史无双卡和失败计划保留。

#### 部署或回滚注意事项

- 旧服务端不提供 `activePlans` 时前端继续按原历史流程工作；部署新能力需同时更新前端与 CLI 服务。

### 完善计划卡生命周期折叠（`f99f48b`）

#### 问题或目标

- 已完成卡长期展开会持续占位；失败、中断或未完成卡若立即消失又不利于用户判断是否继续。

#### 实现内容

- 增加 `live`、`completed`、`failed`、`interrupted` 和 `incomplete` 状态标签与折叠摘要。
- 运行中默认展开，完成后默认收起，失败/中断在当前轮保持展开，下一轮用户消息出现后收起。
- 手动展开/收起按线程和轮次保存到 `localStorage`，最多 200 条；历史 `turnError` 可恢复失败标签。

#### 用户影响

- 完成卡只占一行主体空间，点击可查看详情与 `Implement plan`。
- 无论用户发送“继续”还是其他新任务，都以新 `turnId` 作为收起旧卡的边界，新计划使用独立卡片。

#### 测试验证

- SSR 测试覆盖运行展开、完成折叠、失败保留、下一轮收起和历史错误推断。
- Playwright 在 `13511`、`1440x900` 实测浅色展开与深色折叠状态，手动展开和收起在刷新后均保持。

#### 部署或回滚注意事项

- 回滚不需迁移数据；旧的本地折叠键在代码回退后仅会被忽略。

### 补充计划恢复与折叠验收文档（`b841e93`）

#### 问题或目标

- 快照、实时流、历史交接和下一轮收起涉及多阶段状态，需要可重复的人工验收步骤。

#### 实现内容

- 新增硬刷新、历史延迟、无双卡、终态 30 秒边界、继续/新任务、本地折叠、请求数和明暗主题测试矩阵。
- 已完成计划的 `Implement plan` 验收步骤改为先展开折叠卡。

#### 用户影响

- 无运行时变化；验收人员可以明确判定每个状态的预期行为。

#### 测试验证

- 确认 `tests/chat-composer-rendering/` 实际包含 40 个详细测试文件，根索引数量已同步。

#### 部署或回滚注意事项

- 仅文档变更，无部署与数据风险。

### 限制计划快照握手总负载（`8d06ba8`）

#### 问题或目标

- 仅限制 64 个快照和单项 64 KiB 时，极端 `ready.activePlans` 仍可能增长到数 MiB。

#### 实现内容

- 增加 512 KiB 全局序列化预算，更新后同时按数量与字节上限淘汰最旧快照，优先保留最近活动的计划。

#### 用户影响

- 常规计划无变化；大量并发长计划也不会让刷新握手无上限变大。

#### 测试验证

- 全量单元测试 46 个文件、521 项全部通过；`pnpm run build` 的 Vue 类型检查、Vite 生产构建与 CLI 构建通过。
- 13511 真实硬刷新中，活动快照在 170 ms 出现且早于延迟 5 秒的 `thread/read`；历史到达后仍只有 1 张卡。
- 单次刷新只建立 1 条 `/codex-api/ws`，空快照 `ready` 帧为 203 bytes，新增 plan HTTP/RPC 请求为 0；当前线程完整 `thread/read` 仅1 次。

#### 部署或回滚注意事项

- 需重启 WebUI 进程才能加载新的内存容量策略；回滚不涉及持久化数据。

## 2026-08-18

### 新增新聊天默认模型服务端存储（`4ec5a0b`）

#### 问题或目标

- 新聊天默认模型和推理强度需要在同一 WebUI/CODEX_HOME 下跨浏览器共享，不能仅依赖当前页面或 `localStorage`。

#### 实现内容

- 新增版本化的 `linux-codex-webui-new-chat-defaults.json`，按归一化 provider ID 分别保存 `model` 和 `reasoningEffort`。
- 新增 `GET/PATCH /codex-api/preferences/new-chat-defaults`，支持单字段删除、进程内串行、跨进程锁、原子替换和损坏文件备份恢复。

#### 用户影响

- 本提交提供持久化基础；不同浏览器可以读取同一组 provider 新聊天默认值。

#### 测试验证

- 覆盖默认状态、provider 归一化、独立字段更新/删除、并发合并、损坏文件恢复、非法请求和 HTTP 路由。

#### 部署或回滚注意事项

- 部署需同时更新前端与 CLI 服务端并重启 WebUI；回滚代码时可保留偏好文件，之后重新部署仍可复用。

### 接入 provider 级新聊天默认状态（`1fb3c7a`）

#### 问题或目标

- 新聊天默认值必须与当前草稿手动选择、Codex 运行配置和已有线程偏好建立明确优先级。

#### 实现内容

- 新建草稿按“手动选择 → provider 服务端默认 → Codex 配置 → 模型目录回退”解析。
- 推理强度根据当前模型能力校验；不支持时使用模型默认或首个支持值，能力未知时不过度限制。
- 已有线程继续以服务端线程偏好为最高优先级，新聊天默认值不参与覆盖。

#### 用户影响

- 新聊天会稳定使用已保存默认值；当前草稿中手动切换后，普通刷新不会反向覆盖用户选择。

#### 测试验证

- 增加按 provider 读取、首次发送参数、能力回退、旧线程隔离、单 PATCH 且不刷新模型目录等回归用例。

#### 部署或回滚注意事项

- 无已保存字段时自动跟随 Codex 配置，不要为升级手工预写偏好文件。

### 统一新聊天入口并增加默认设置（`e1a9570`）

#### 问题或目标

- 顶部铅笔此前会继承当前项目，与“全局新建默认进入 Chats”的预期不一致，同时界面缺少可配置的新聊天模型入口。

#### 实现内容

- 顶部铅笔和 Chats 铅笔统一清空项目目录，首次发送由服务端创建 projectless Chats 目录；项目铅笔仍使用所点项目的精确目录。
- 设置面板增加新聊天默认模型和推理强度自定义下拉框，支持“跟随 Codex 配置”与能力感知选项。
- provider 行显示当前实际 provider，并补充中文文案与明暗主题错误态。

#### 用户影响

- 全局和 Chats 入口新建的聊天出现在 Chats；项目内铅笔新建的聊天仍出现在原项目。
- 默认模型和推理强度可以从设置直接调整，不需要修改前端本地存储。

#### 测试验证

- Playwright 在 `http://127.0.0.1:13511/`、`1440x900` 下确认顶部和 Chats 入口清空项目目录，项目入口选中 `/root/codex工作目录`。
- 两个独立浏览器会话确认 `myproxy` 的 `GPT-5.6-Sol / Low` 默认值跨浏览器恢复，验收后已还原测试前服务端值。
- 浅色和深色截图均无浅色表面泄漏，深色设置面板实测背景为 `rgba(27, 35, 47, 0.98)`。

#### 部署或回滚注意事项

- 部署需同时更新 `dist` 与 `dist-cli` 并重启 WebUI。回滚界面时服务端偏好文件可保留；旧前端不会读写它。

### 修复设置下拉菜单的 Teleport 点击边界（`3795fac`）

#### 问题或目标

- 设置中的自定义下拉菜单渲染到 `body` 后，面板外部点击监听会在选项 `click` 前关闭面板，使选择无法生效。

#### 实现内容

- 将 `.composer-dropdown-menu-wrap` 识别为设置面板自有浮层，保留真实外部点击关闭行为。

#### 用户影响

- 新聊天默认值以及设置内其他自定义下拉框可以正常选择和保存。

#### 测试验证

- 两次设置选择各产生一个 PATCH，请求体 46–48 bytes，响应体 83–107 bytes；选择期间模型目录和 `thread/list` 新增请求均为 0。
- 全量单元测试 44 个文件、508 项全部通过；`pnpm run build` 的 Vue 类型检查、Vite 生产构建和 CLI 构建全部通过。
- 性能审计确认：每次页面加载只读取一次新聊天默认状态；设置修改不阻塞线程列表，不引入轮询、无界并发、大载荷或缓存失效。

#### 部署或回滚注意事项

- 修复仅收窄设置面板的外部点击判定，不改变 ComposerDropdown 定位与其他浮层层级；回滚后新聊天设置选择将再次无法提交。

## 2026-08-17

### 显示聊天消息日期（`2e30022`）

#### 问题或目标

- 用户和助手消息缺少可见日期，长对话中难以判断发送与回复的具体时间。

#### 实现内容

- 为 `UiMessage` 增加 `timestampIso`，历史轮次使用 Codex `startedAt/completedAt`，实时消息在出现时记录时间并在持久化后与服务端数据对齐。
- 聊天界面仅在用户和助手消息下显示 `YYYY-MM-DD HH:mm`，命令、文件变更、进度和系统提示不增加无关标签。

#### 用户影响

- 新旧聊天、流式回复、计划卡和生成图片都能显示对应日期，更容易追溯对话时间。

#### 测试验证

- normalizer、聊天组件和即时发送状态回归测试通过；`pnpm run build` 通过。
- 13511 桌面浅色与手机深色真实界面验收通过。

#### 部署或回滚注意事项

- 不新增 RPC、轮询或持久化格式；原生分页 item-only 回退会从 UUIDv7 `turnId` 推导开始时间。
- 回滚代码即可移除日期显示，无数据清理需求。

### 新增侧栏偏好服务端存储（`e076aa3`）

#### 问题或目标

- 侧栏总区和单项目的展开收起状态此前只保存在浏览器 `localStorage`，同一 WebUI 实例换浏览器后无法恢复用户已经点击过的布局。
- 整份偏好覆盖式写入还会使两个浏览器对不同项目的操作互相丢失，需要服务端字段级合并。

#### 实现内容

- 新增版本化的 `linux-codex-webui-sidebar-preferences.json`，存放 Pinned、Chats、Projects 总区状态和按项目键记录的收起状态。
- 写入使用进程内串行队列、跨进程锁、0600 临时文件和原子替换；损坏 JSON 会先备份再恢复默认值。
- 新增 `GET/PATCH /codex-api/preferences/sidebar-layout`，支持单字段合并、最多一次的旧浏览器状态初始化和服务端 revision。

#### 用户影响

- 本提交只建立服务端能力，不单独改变界面；完成前端接入后，同一 `CODEX_HOME` 的不同浏览器可以共享侧栏折叠状态。
- 不修改 Codex 自身的 `.codex-global-state.json`，也不影响置顶聊天、项目顺序或线程历史。

#### 测试验证

- 覆盖默认状态、首次初始化保护、并发字段合并、项目展开删除、损坏文件恢复、非法请求和 HTTP 路由。
- 服务端定向测试 2 个文件、8 项通过；CLI 构建通过。

#### 部署或回滚注意事项

- 部署需重新构建并重启 WebUI 服务；偏好文件缺失时默认全部展开，首次正常请求会自动创建。
- 回退本提交并删除仅由测试实例生成的侧栏偏好文件即可移除服务端能力；不要删除正式实例文件，除非明确希望重置所有侧栏状态。

### 同步侧栏折叠状态并固定聊天优先（`190be63`）

#### 问题或目标

- Chats 默认位于 Projects 下方，且 `Chats first`、总区展开状态、单项目折叠状态分别依赖当前浏览器本地值，无法形成稳定一致的侧栏布局。

#### 实现内容

- 模板和视觉顺序固定为 `Pinned → Chats → Projects`，移除 `Chats first` 菜单和旧排序值的影响，聊天内部仍保留 Created/Updated 排序。
- 首次加载在服务端无状态时迁移旧 `localStorage`；服务端已有状态时以服务端为准，防止另一浏览器的旧值反向覆盖。
- 用户点击立即乐观更新，随后串行发送字段级 PATCH；浏览器重新获得焦点时 GET 最新状态。路由、搜索、线程刷新和项目排序不会自动改变折叠状态。

#### 用户影响

- 有置顶聊天时 Chats 始终位于置顶区下方；没有置顶聊天时 Chats 位于侧栏记录区最上方。
- Pinned、Chats、Projects 总区及每个项目的展开收起会在访问同一 WebUI 实例的浏览器间保持一致。
- `Show more/Show less` 仍是临时分页状态，不与项目折叠混合持久化。

#### 测试验证

- 全量单元测试 42 个文件、495 项通过；`pnpm run build` 的 Vue 类型检查、Vite 前端构建和 CLI 构建通过。
- Playwright 在 `http://127.0.0.1:13511/`、`1440x900` 下确认 DOM 与视觉顺序均为 Pinned、Chats、Projects；浏览器 A 收起项目后浏览器 B 正确恢复，B 展开后 A 聚焦同步，刷新后状态保持。
- 明暗主题截图均确认侧栏表面、标题、箭头和项目行可读；暗色页面未出现浅色侧栏表面。
- 性能检查：两次项目点击和两次总区点击各自产生且仅产生一个 PATCH；请求体 28–49 bytes、响应体 146–172 bytes、实测 89–154 ms，状态文件 146 bytes。两浏览器首次加载、聚焦同步及两次刷新共 5 个 GET，未增加 `thread/list`、历史读取或无界轮询。

#### 部署或回滚注意事项

- 部署需同时更新 `dist` 与 `dist-cli` 并重启 WebUI；不同 WebUI 实例或不同 `CODEX_HOME` 不共享偏好，这是当前单用户本地服务边界。
- 回退 `190be63` 会恢复浏览器本地折叠和可选 Chats first；服务端偏好文件可以保留，之后重新部署本功能时仍可复用。

## 2026-08-03

### 使用发送模型自动生成新聊天标题（`ba77ad1`）

#### 问题或目标

- 新建聊天原先直接截取用户首条消息作为标题；当消息以 GitHub 链接、命令或很长的需求开头时，侧栏标题难以辨认，用户常常需要手动重命名。

#### 实现内容

- 在首条消息成功发送后，后台使用该次发送实际采用的模型执行隔离的 `codex exec --ephemeral` 标题生成任务，不创建额外 Codex 聊天或历史记录。
- 标题任务单并发、最多排队 8 个、超时 25 秒；生成失败时保留原有临时标题，不阻塞聊天发送和回复。
- 手动重命名优先级最高，后台生成结果不会覆盖用户已经修改过的名称。

#### 用户影响

- 新聊天会自动获得更能概括任务的中文标题；链接、命令和长提示不再直接占据侧栏标题。
- 正常聊天发送、模型选择、历史记录和手动重命名方式保持不变。

#### 测试验证

- 覆盖实际发送模型传递、后台队列、超时、失败回退、手动重命名优先和不写入聊天历史的回归用例。
- 最终联合验证 `pnpm run test:unit` 通过（40 个文件、483 项），`pnpm run build` 通过。
- 性能检查：标题生成脱离前台发送链路且限制为单并发队列；常规已存在聊天的打开、历史加载与模型偏好读取不新增请求。

#### 部署或回滚注意事项

- 部署需要同时更新 `dist` 与 `dist-cli` 并重启目标 WebUI 服务；不涉及聊天数据迁移。
- 回退 `ba77ad1` 并重新构建即可恢复原有临时标题策略，已生成的标题不会被删除。

### 修复跨设备会话模型偏好覆盖（`0c26899`）

#### 问题或目标

- 手机端以 `GPT-5.6-Sol / Max` 创建或发送的聊天，桌面端打开时会把本机默认的 `GPT-5.6-Terra` 和推理强度写回服务端，导致同一聊天在不同设备显示和下次发送参数不一致。

#### 实现内容

- 移除“打开既有聊天时自动保存本机默认模型和推理强度”的回写路径。
- 仅在 `turn/start` 成功且请求明确携带 `model` 与 `effort` 时，服务端持久化该聊天的模型偏好；新聊天复用同一逻辑。
- 用户在界面中手动选择模型或推理强度时的即时保存逻辑保持不变。

#### 用户影响

- 手机和桌面重新打开同一聊天时都会显示并使用该聊天真实保存的模型和推理强度，不会再被当前设备默认值覆盖。
- 后续仍可随时手动调整模型和推理强度，新的有效选择会照常保存到该聊天。

#### 测试验证

- 新增既有聊天打开不回写、成功发送后持久化、手动选择保持有效等服务端与前端回归测试。
- `pnpm run test:unit` 通过（40 个文件、483 项），`vue-tsc --noEmit`、Vite 前端构建和 CLI 构建通过。
- 使用独立手机与桌面浏览器状态验收：手机设为 `5.6-Sol / Max` 后，桌面首次打开、刷新以及深色主题均保持 `5.6-Sol / Max`；错误偏好回写请求计数为 0。
- 性能检查：删除既有聊天打开时的自动写入，不新增轮询、启动扫描或历史加载请求。

#### 部署或回滚注意事项

- 部署需要同时更新 `dist` 与 `dist-cli` 并重启目标 WebUI 服务；不改变已有聊天的模型偏好存储格式。
- 回退 `0c26899` 并重新构建即可恢复旧行为，不需要数据迁移；已保存的聊天偏好会保留。

## 2026-07-30

### 修复截图消息重复显示（`72edd66`）

#### 问题或目标

- 发送带截图和文本的消息后，服务端历史会同时包含图片块及其派生文件附件；前端在乐观消息与历史消息合并时把两者误认为不同消息，造成同一段用户文字显示两次。

#### 实现内容

- 调整乐观用户消息与历史用户消息的等价判定：若文件附件与本地图片 URL 指向同一路径，将其识别为图片的派生附件，不再计入独立附件差异。
- 保留图片块、独立文件附件、远程图片和纯文本消息的原有传输及展示逻辑。

#### 用户影响

- 带截图的聊天消息在服务端确认和历史刷新后只显示一条，确认后的消息仍完整保留截图及附件信息。

#### 测试验证

- `pnpm exec vitest run src/composables/useDesktopState.test.ts` 通过（124 项）。
- `pnpm run test:unit` 通过（38 个文件、479 项）；`pnpm run build:frontend` 通过。
- 性能检查：仅收紧前端本地等价比较，不新增 API、轮询、历史加载或附件上传请求。

#### 部署或回滚注意事项

- 不改变 RPC 参数、服务端接口或已存聊天数据；重新构建前端即可生效。
- 如需回退，回退 `72edd66` 并重新构建，不需要迁移数据。

### 移除长任务消息窗口裁剪（`cb01c82`）

#### 问题或目标

- 长任务持续输出时，聊天前端会只保留最近 50 条已加载消息；移动端向上浏览时，本地补回窗口可能未触发，导致顶部出现空白，必须刷新页面才能重新看到内容。

#### 实现内容

- 删除 `ThreadConversation` 的 50 条前端渲染窗口、本地窗口起点维护及本地分批回填逻辑。
- 直接渲染当前已加载的完整消息列表；仅在确有更早持久化历史时，保留原有的服务端分页加载与滚动位置恢复。

#### 用户影响

- 长任务运行期间，已经加载的聊天内容不会再被前端从顶部裁掉；向上滚动可持续阅读，不会出现只能刷新恢复的空白区。

#### 测试验证

- `pnpm run build:frontend` 通过；`pnpm run test:unit` 通过（38 个文件、479 项）。
- 代码确认不再存在 `RENDER_WINDOW_SIZE`、`LOAD_MORE_CHUNK`、`renderWindowStart` 或 `visibleMessages` 的窗口裁剪引用。
- 生产构建中 `ThreadConversation` 产物从 89,764 bytes 降至 89,474 bytes；不增加 API 请求，命令输出按需展开和服务端历史分页仍保留。

#### 部署或回滚注意事项

- 不修改 RPC、历史分页协议、自动跟随、命令折叠或文件操作；已加载消息较多时 DOM 行数会相应增加，这是为确保可见性作出的明确取舍。
- 如需回退，回退 `cb01c82` 并重新构建即可恢复旧窗口策略，无需数据迁移。

## 2026-07-28

### 修复手机侧栏线程菜单无法点击（`c74a0e7`）

#### 问题或目标

- 手机端侧栏以抽屉形式显示时，线程三点菜单会 Teleport 到 `body`；菜单仍使用普通浮层层级 `30`，低于抽屉遮罩层级 `40`，所以菜单虽已生成却无法被触摸命中。

#### 实现内容

- 仅将固定定位的线程菜单提升为 `calc(var(--ui-z-drawer) + 1)`，确保它位于侧栏抽屉之上、`z-index: 50` 的模态框之下。
- 补充手机触摸回归用例：打开三点菜单后检查其覆盖抽屉遮罩，并点击 `Copy path` 验证操作不会关闭侧栏。

#### 用户影响

- 手机端首次点击聊天条目右侧三点即可打开并操作菜单；菜单动作不再被遮罩截走，也不会意外关闭侧栏。

#### 测试验证

- Playwright 在 `375x812` 和 `768x1024` 的浅色、深色主题中均确认菜单层级为 `41`、抽屉为 `40`，菜单命中目标正确，点击 `Copy path` 后菜单关闭且手机侧栏仍保持打开。
- 全量单元测试 38 个文件、478/478 项通过；`pnpm run build` 通过。浏览器性能报告无重复请求警告，首屏 API 总量约 158 KiB；该修复仅调整 CSS 层级，未增加请求、轮询或状态工作。

#### 部署或回滚注意事项

- 不改变 API、RPC、线程数据、用户偏好或持久化格式；部署需重新构建并重启目标 WebUI 服务。
- 回退 `c74a0e7` 并重新构建即可恢复旧层级，无需数据回滚。

### 修复跨实例幽灵轮次与停止报错（`9dcd1bb`）

#### 问题或目标

- 旧 WebUI 实例可能持续刷新一个缺少 `task_started` 生命周期证据的幽灵轮次租约，使当前实例在真实任务完成后仍显示运行中；此时点击停止还会因当前 app-server 未接管该线程而显示 `thread not found` 红色错误。

#### 实现内容

- 外部运行租约必须在 15 秒确认窗口内获得对应会话的 `task_started` 证据，才能继续作为长期运行依据；同时保留真实长时间静默任务和通知短暂延迟的兼容路径。
- 将 `thread not found` 纳入停止竞态恢复：立即读取单线程权威运行态，并在任务已经结束时清理进度卡和错误状态。

#### 用户影响

- 任务完成后，其他端口遗留的幽灵心跳不会再复活运行进度；停止按钮与终态同步发生竞态时，也不会再留下原始 502 红色错误。

#### 测试验证

- 新增跨实例幽灵租约、确认窗口和 `thread not found` 回归测试；定向测试 148/148、全量单元测试 478/478 通过，`vue-tsc`、Vite 与 CLI 构建通过。
- 性能检查确认未新增轮询或常规请求；仅停止竞态路径执行一次已有的单线程运行态核对。

#### 部署或回滚注意事项

- 不改变 RPC 协议、租约版本或用户数据；代码部署需要重新构建并重启目标实例，但本次 GitHub 推送本身不重启 13510/13511。
- 如需回滚，可回退 `9dcd1bb` 并重新构建；真实拥有 `task_started` 的跨实例长任务行为不受影响。

### 修复排队交接的最后一帧空窗（`232d687`）

#### 问题或目标

- 持久化用户消息进入前端状态后，发送中队列行会立即被过滤，但对话 DOM 可能晚一个事件循环提交；快速连续排队时仍可能出现最后一帧空白。

#### 实现内容

- 按线程记录已完成渲染交接的队列 ID；匹配到真实 `turnId` 后等待下一次浏览器任务，再释放对应的发送中行。
- 异步队列刷新在释放确认前继续保留服务端 claim，并在队列清理时同步回收计时器和交接 ID。

#### 用户影响

- 排队消息从“发送中”切换为正式用户消息时始终至少有一个可见副本；慢历史加载和快速连续轮次下不再闪出空白。

#### 测试验证

- `useDesktopState` 122 项测试、全量 475 项测试、`vue-tsc --noEmit` 和 `pnpm run build` 均通过。
- 性能检查确认未新增网络请求、全局轮询或无界状态；新增计时器和 ID 均在线程队列清理时回收。

#### 部署或回滚注意事项

- 不修改后端协议、持久化格式或依赖；部署需重建前端资源，回退 `232d687` 即可恢复此前的交接时机。

### 修复同名分支路径导致的提交列表错误（`f56437e`）

#### 问题或目标

- 仓库同时存在 `dev` 分支和名为 `dev` 的文件或目录时，`branch-commits` 接口执行 `git log dev` 会产生 revision/path 歧义，聊天页因此周期性收到 500。

#### 实现内容

- 在所有 revision 与 reset-history ref 后加入 `--` 分隔符，明确 Git 参数边界。
- 执行 Git 命令前验证 `cwd`，将不存在目录、非 Git 目录和未知 ref 分别映射为 404、422 和 404。

#### 用户影响

- 同名分支与路径不再阻塞 Git 下拉菜单的提交列表，也不会持续产生该接口的 500；无效目录或分支会返回可判断的具体错误。

#### 测试验证

- 使用真实临时 Git 仓库验证 `dev` 分支与同名路径共存时返回 200 和正确提交；未知分支返回 404、非 Git 目录返回 422、不存在目录返回 404。
- 专项 Vitest、`vue-tsc --noEmit` 和 `git diff --check` 通过；性能检查确认正常请求没有增加 Git 子进程或额外接口调用。

#### 部署或回滚注意事项

- 不改变正常分支、远端 ref、最多 50 条提交限制或数据格式；部署服务端构建后生效，回退 `f56437e` 可恢复旧参数处理。

### 修复排队消息交接空窗与竞态（`89330b5`）

#### 问题或目标

- 后台从持久队列取出消息后，前端会先移除排队行，再等待历史同步生成用户消息，产生约 203 至 407 毫秒的可见空窗；启动失败、断流、重连和陈旧页面写回还可能造成顺序漂移或错误覆盖。

#### 实现内容

- 为队列项增加服务端持有的 `claimed`、`claimedAtMs` 和 `turnId` 状态：领取时原位标记，由 RPC 或 `turn/started` 确认真实轮次，精确终态后完成，启动失败则原位恢复。
- 遗留 claim 分别通过 legacy 或 paginated 历史核对，确认缺失且超时后才重试；前端保留禁用的发送中行，直到同一 `turnId` 的用户消息已持久化，并按线程合并队列刷新。

#### 用户影响

- 排队消息在进入聊天记录前始终可见；发送中消息不可编辑、删除、拖动或立即发送，相同文本、附件、技能、模型、推理强度和速度设置保持不变。

#### 测试验证

- Vitest 覆盖极短交接、相同文本、附件与技能、启动失败、RPC 断流、重连、legacy/paginated 历史和陈旧写回，共 205 项通过；`vue-tsc --noEmit` 与 `git diff --check` 通过。
- 性能检查确认未新增全局轮询；仅遗留 claim 恢复路径按需读取最多十个历史轮次，常规发送路径不增加历史请求。

#### 部署或回滚注意事项

- 旧队列数据可继续读取，新增字段可被旧版本忽略，无需迁移；服务端拒绝无主的陈旧 claimed 行，避免已完成项目被旧页面复活。
- 回退 `89330b5` 并重新构建可恢复旧队列交接逻辑，不涉及聊天记录迁移。

### 修复未确认轮次残留与停止误报（`423eab3`）

#### 问题或目标

- `turn/start` 的 RPC 返回 ID 在缺少 `turn/started` 或会话 `task_started` 证据时仍被当作真实轮次并续租，导致真实轮次结束后进度卡长期停在运行中，点击停止又出现 `no active turn` 红色错误。

#### 实现内容

- 为本地运行证据增加生命周期确认状态和 15 秒确认窗口；RPC 返回只建立临时证据且不写跨实例租约，收到 `turn/started` 或 `task_started` 后才提升为长期运行。
- 真实终态可以覆盖较新的未确认 ID；已确认的新轮次和带新鲜心跳的长时间静默任务仍保持运行。前端遇到未确认 ID 与终态不一致时，立即执行一次单线程权威核对并清理幽灵状态。

#### 用户影响

- 任务完成后进度卡会及时结束、发送按钮恢复；已经结束的幽灵任务不再保留停止按钮或原始 RPC 红色错误，真实的新轮次也不会被旧终态误关。

#### 测试验证

- `src/server/threadRuntimeState.test.ts` 与 `src/composables/useDesktopState.test.ts` 定向测试共 143 项通过，`pnpm run build` 通过。
- 性能检查确认没有增加全局轮询；仅未确认 ID 与终态不一致的罕见路径增加一次单线程运行态请求。

#### 部署或回滚注意事项

- 运行态租约格式保持 version 1，旧租约可继续读取；无需数据迁移，回退 `423eab3` 并重新构建可恢复此前确认策略。

### 修复停止任务时过期 turn ID 报错（`2e76b15`）

#### 问题或目标

- 页面缓存的 active turn ID 被 Codex app-server 的新轮次替换时，停止任务会收到 `expected active turn id` 的 502，并在聊天中留下红色错误卡片。

#### 实现内容

- 仅识别 app-server 明确返回的 active turn ID 不一致错误；发生时重新读取该线程运行态，并使用确认后的新 turn ID 重试一次中断请求。
- 成功后的本地进度、终态记录和活动 ID 清理均以实际中断的轮次为准。

#### 用户影响

- 点击停止不再因页面缓存的旧 turn ID 与后端当前轮次不一致而直接报错；极短的换轮次竞态会自动完成一次安全重试。

#### 测试验证

- `pnpm exec vitest run src/composables/useDesktopState.test.ts` 118/118 通过；`pnpm run build` 的 `vue-tsc`、Vite 和 CLI 构建全部通过。
- 性能检查确认正常停止路径不增加请求；仅 ID 不一致错误路径增加一次单线程运行态查询和一次中断重试。

#### 部署或回滚注意事项

- 不改变轮询频率、缓存策略、RPC 协议或其他会话流程；回退 `2e76b15` 并重新构建可恢复旧停止行为。

## 2026-07-24

### 修复桌面访问权限菜单错位（`8cb2700`）

#### 问题或目标

- 桌面端权限菜单会因聊天容器的 `backdrop-filter` 定位上下文而偏离触发按钮，部分选项落到可视区域外，无法点击。

#### 实现内容

- 将 Composer 权限菜单 Teleport 到 `body`，并调整外部点击判断以识别顶层菜单节点。

#### 用户影响

- 桌面端“访问权限”可正常展开、选择和关闭；手机独立设置 Sheet 不变。

#### 测试验证

- 单元测试、生产构建及 `1440x900` 明暗主题浏览器点击与定位验收均通过。

#### 部署或回滚注意事项

- 不改变权限值、接口或持久化格式；回滚该提交即可恢复旧浮层挂载方式。

### 移除聊天顶部历史消息按钮（`6933620`）

#### 问题或目标

- 常驻“加载更早的消息”按钮会占据任务进度卡上方空间，干扰运行监看。

#### 实现内容

- 移除常驻按钮、相关样式和无用翻译；保留滚动到聊天顶部时按批次加载更早消息及滚动位置恢复。

#### 用户影响

- 聊天内容与任务进度直接相邻；查看更早记录改为自然向上滚动。

#### 测试验证

- 34 个测试文件、384 项单元测试和生产构建通过；代码路径确认未增加请求或监听器。

#### 部署或回滚注意事项

- 不修改消息接口、缓存或任务状态；回滚仅恢复顶部按钮。

### 移除打开聊天时的主动恢复（`42354ef`）

#### 问题或目标

- 仅查看历史时调用 `thread/resume` 会物化运行态和大量命令输出，拖慢长聊天首屏。

#### 实现内容

- 历史加载统一使用只读 `getThreadDetail`；发送、重试等真正需要继续线程的路径仍按需调用 `resumeThread`。

#### 用户影响

- 打开旧聊天不再触发线程恢复，历史更快出现；发送行为保持兼容。

#### 测试验证

- `useDesktopState` 定向测试 96 项通过，覆盖打开不恢复、发送前恢复及调用顺序。

#### 部署或回滚注意事项

- 不改变 legacy 历史协议、服务配置或用户数据；回滚该提交可恢复旧恢复时机。

### 补充按需恢复线程的验收步骤（`bc1494a`）

#### 问题或目标

- 性能优化后需要明确区分“只读打开”和“发送时恢复”的网络验收标准。

#### 实现内容

- 手工用例明确规定：打开阶段只出现 `thread/read`，发送阶段才依序出现 `thread/resume` 与 `turn/start`。

#### 用户影响

- 后续维护可直接通过请求顺序确认聊天打开性能未牺牲发送能力。

#### 测试验证

- 文档改动通过 `git diff --check`，相关自动化测试 96 项通过。

#### 部署或回滚注意事项

- 仅更新验收文档，不影响运行时或部署；回滚只会移除该说明。

### 接入原生分页历史并加速聊天打开（`c858a6d`）

#### 问题或目标

- WebUI 需兼容 Codex 原生 paginated 历史，并解决长聊天首次打开慢、重复读取和大命令输出阻塞首屏的问题。

#### 实现内容

- 建立 legacy/paginated 双历史链路，接入 `thread/turns/list`、`thread/items/list` 与不透明游标；加入请求合并、20 线程 LRU、加载竞态隔离及首次 `ready` 去重。
- 命令输出首屏限制为单项 256 KiB、单响应 512 KiB；截断输出展开后按需读取完整内容并缓存最多 8 项；消息交互身份统一为 `turnId + itemId`。

#### 用户影响

- Legacy 打开只读一次历史而不主动恢复；分页线程可加载最新与更早页面；返回已打开聊天可直接命中缓存；超大命令输出不再阻塞首屏。
- 官方尚不支持的 paginated fork、回滚和编辑会明确禁用，legacy 行为保持不变。

#### 测试验证

- 全量 `pnpm run test:unit` 通过 37 个文件、457 项；`pnpm run build` 与 CLI 冒烟通过。
- 浏览器性能验收确认：legacy 为 1 次 `thread/read`、0 次 `resume`；paginated 为 1 次带 `initialTurnsPage` 的 `resume`、0 次完整 `thread/read`；缓存回访无历史请求、无重复历史分页。

#### 部署或回滚注意事项

- 需重新构建前端与 CLI 后重启目标服务；13510、13511 已部署同一构建。回滚 `c858a6d` 并重新构建即可恢复上一版历史链路。

## 2026-07-22

### 修复聊天消息发送后延迟显示（`01e26e2`）

#### 问题或目标

- 新聊天发送首条消息时会等待 `thread/start-turn` 返回真实线程 ID，旧聊天发送时会等待线程恢复或回合启动；接口耗时约一至两秒时，用户文字无法立即显示。
- 刚点开的历史聊天仍在加载详情时，全局加载状态还会遮住已经插入的乐观消息，造成运行状态先出现、发送文字后出现的错序体验。

#### 实现内容

- 旧聊天的非队列发送在后端请求前立即插入乐观用户消息，并在用户主动发送时结束消息加载遮罩；上一回合的陈旧进度会被清理，新的思考状态紧随消息显示。
- 新聊天增加仅在真实线程创建期间存在的一条临时用户消息和运行状态，首页直接复用正式 `ThreadConversation` 渲染；真实线程建立并完成路由后清理临时状态。
- 保留现有真实消息合并去重机制；创建失败时保留用户文字并显示错误，运行中 `steer` 即时显示，`queue` 与工具输入回复仍沿用原有专用区域和响应路径。

#### 用户影响

- 新聊天、旧聊天和刚点开但仍在加载的历史聊天，点击发送后约一个渲染帧即可看到文字，思考或主推理状态位于文字下方，不再单独抢先出现。
- 不改变队列消息、工具输入、模型、权限、附件、技能和线程持久化行为。

#### 测试验证

- `pnpm run test:unit` 通过 34 个测试文件、384 项测试；`pnpm run build` 通过。
- Playwright 在 `1440x900` 浅色、`375x812` 深色以及刚打开的旧线程中验证，用户文字和状态约 100 至 150ms 出现、顺序正确且无重复。
- `PROFILE_BASE_URL=http://127.0.0.1:4173 PROFILE_WAIT_MS=7000 pnpm run profile:browser` 无警告；线程列表、首屏线程读取、技能列表、限额和模型请求未出现重复，未新增网络请求或无界状态。

#### 部署或回滚注意事项

- 不修改 RPC、通知协议、服务配置、队列持久化格式或用户数据；部署时重新构建并重启 WebUI 服务即可。
- 回滚 `01e26e2` 会恢复旧的等待后端结果再显示消息行为，不涉及聊天记录迁移。

### 建立统一的 macOS 视觉基础与桌面框架（`26ff119`）

#### 问题或目标

- 原有界面虽然已有部分 macOS 风格，但字体、材质、层级、焦点反馈和暗色主题并不统一；侧栏、顶栏和首页存在不同节奏的卡片、阴影与交互尺寸。
- 本次目标是在不改变路由、数据状态、接口和服务配置的前提下，建立可供后续页面复用的简白磨砂玻璃基础，并确保触控与无障碍偏好不会被视觉效果破坏。

#### 实现内容

- 新增浅色/深色成对的 macOS 语义 Token，统一背景、侧栏、内容表面、边框、文本、焦点环、状态色和阴影；毛玻璃只用于顶栏、侧栏和少量浮层，滚动内容保持足够不透明。
- 重整桌面框架、顶栏、侧栏导航、首页空状态、设置区和自动化入口；自动化改用独立时钟图标，侧栏宽度拖动改为 Pointer Events，并保留键盘调整能力。
- 加入 `prefers-reduced-transparency`、`prefers-reduced-motion` 与 `prefers-contrast` 的降级样式，避免透明或动效偏好下出现低对比、晕动或不可读的界面。

#### 用户影响

- 桌面、平板与手机获得一致的浅色蓝灰和深色石墨材质层次；导航、空状态、按钮和设置区的视觉关系更清楚。
- 粗指针设备的关键入口更容易点按，主题切换、键盘焦点和辅助功能偏好仍保持可用。

#### 测试验证

- `pnpm run build:frontend` 通过；本次基础层完成后继续执行了全量 `pnpm run test:unit`，34 个测试文件、377 项测试均通过。
- 最终 Playwright 覆盖 `375x812`、`768x1024`、`1440x900` 的浅色和深色共 6 组，页面无横向溢出、控制台错误或页面错误。

#### 部署或回滚注意事项

- 不修改 API、RPC、路由、线程数据、用户偏好格式或服务单元；重新构建前端资源即可部署。
- 回滚 `26ff119` 会恢复旧的全局视觉基础，不会删除聊天、项目或设置数据。

### 重做手机输入设置 Sheet 与浮层交互（`29f8958`）

#### 问题或目标

- 手机上的权限、模型和推理菜单此前会在设置面板上继续叠加固定定位浮层，尤其在 640 至 767px、软键盘和长选项场景容易形成遮挡、错层或无法点击。
- 需要保留真实模型能力、权限、Fast、Plan、附件、语音、输入框自动增高和 Enter 发送等原有功能。

#### 实现内容

- 将手机设置收敛为单一 Sheet：根页展示权限、模型、推理三项，二级页直接使用既有实时选项数据，不再叠加桌面模型菜单。
- 增加焦点陷阱、Escape 关闭、背景 `inert`、滚动锁定和关闭后回到设置触发器的焦点恢复；Skills 打开时保持不自动聚焦搜索框，避免再次弹出输入法。
- 统一 Composer 下拉组件的 ARIA 状态、`visualViewport` 定位与触控尺寸；保留 768px 及以上的桌面 Composer 布局。

#### 用户影响

- 手机端设置始终只出现一个清晰的面板，权限、模型和推理可逐级进入并正常选择；关闭后不会把焦点错误送回聊天输入框。
- Skills 列表可先完整查看，用户主动点击搜索才唤起输入法；桌面原有模型级联菜单和操作路径不变。

#### 测试验证

- 相关 3 个测试文件共 16 项通过，`pnpm run build:frontend` 通过。
- Playwright 验证手机 Sheet 只有一层、三个设置行均高于 44px、背景锁定/解除正确、关闭后焦点回到设置按钮，且 768px 仍使用桌面控件。

#### 部署或回滚注意事项

- 不新增依赖、网络请求或持久化字段，不改变实时模型能力、权限或聊天发送协议。
- 回滚 `29f8958` 仅恢复旧手机浮层交互，已有聊天、模型和偏好数据不受影响。

### 完善对话内容与关键弹窗的 macOS 体验（`25e54ae`）

#### 问题或目标

- 对话消息、代码、命令输出、任务进度、Diff、归档删除、技能详情和项目导出仍使用不一致的材质与层级；同时要避免牺牲长内容滚动、键盘操作和手机触控。

#### 实现内容

- 统一用户消息、代码窗口、命令行、进度卡和关键弹窗的圆角、边框、状态色与层级；代码块增加 macOS 窗口细节，运行/异常状态保持清晰可辨。
- 移除命令展开的布局属性动画，ZIP 进度改用 `transform`；保留超长 Diff 的横向滚动，避免视觉优化裁掉长行。
- 为技能详情、归档永久删除和 ZIP 导出补齐 Dialog 语义、焦点陷阱、背景 `inert`、滚动锁和关闭后的焦点恢复；粗指针下命令、复制、文件操作和 Diff 控件统一到至少 44px。

#### 用户影响

- 对话内容层次更清晰，代码、命令和进度卡更易阅读；手机端复制、展开、文件操作和危险操作的命中区更可靠。
- 弹窗不会点穿背景，浅色与深色下的删除、错误、运行和完成状态均保持清楚。

#### 测试验证

- `AppModals`、`ProjectZipExportModal`、`CommandExecutionBlock` 共 3 个测试文件、11 项测试通过，`pnpm run build:frontend` 与 `git diff --check` 通过。
- 最终线程验收覆盖真实消息、命令、进度卡和长 Diff；未发现浅色表面串入深色、重叠或页面级横向溢出。

#### 部署或回滚注意事项

- 不修改任务状态、RPC、归档、文件操作或 ZIP 导出协议；不新增依赖和请求。
- 回滚 `25e54ae` 可单独恢复该批对话与弹窗样式，不影响已有会话和导出数据。

### 统一目录、Review 与终端的次级工作区体验（`0e0e71b`）

#### 问题或目标

- Directory、Review 与终端仍会受到系统暗色覆盖、离散浮层层级、手机按钮过小和固定高度计算的影响，和主聊天区的 macOS 体验不一致。

#### 实现内容

- Directory 仅跟随应用 `.dark` 主题，补齐暗色详情和状态徽标；目录弹窗与 Review 统一圆角、层级和非对称 Sheet 动效。
- Review 的分支控件、动态视口高度和安全区适配完善；终端改为 flex 填充和 `dvh` 高度，移除脆弱的固定高度减法。
- 手机排序、详情、Review 和终端的关键动作统一为至少 `44x44px`。

#### 用户影响

- 应用显式选择浅色时不会再被系统暗色污染；深色详情文字、徽标和操作控件更清晰。
- 软键盘、底部安全区和不同手机视口下，Review Sheet 与终端不会拥挤、被底部遮挡或失去操作入口。

#### 测试验证

- `directoryHubUtils` 与 `terminalManager` 共 2 个测试文件、10 项测试通过；`pnpm run build:frontend` 与 `git diff --check` 通过。
- 浅色和深色 Directory 成图复核无主工作区重叠或异常裁切；独立只读复核未发现阻断问题。

#### 部署或回滚注意事项

- 不改变目录数据、Review 加载、Git 操作或终端会话协议，不新增依赖或请求。
- 回滚 `0e0e71b` 仅恢复旧的次级工作区布局，不影响项目和终端数据。

### 扩充 macOS 视觉刷新手工验收矩阵（`bd7db4e`）

#### 问题或目标

- 本轮改动覆盖 Composer 单层 Sheet、对话工具、关键弹窗、Directory、Review 与终端，原有手工用例不足以持续验证焦点、触控、动态视口和辅助功能降级。

#### 实现内容

- 扩展既有 macOS 界面用例，加入 `375x812`、`767x900`、`768x1024`、`1440x900` 的浅色/深色与粗指针验收步骤。
- 覆盖真实模型设置、长 Diff 横向滚动、弹窗焦点约束、ZIP 导出、主题来源、safe-area、`prefers-reduced-motion` 和 `prefers-reduced-transparency`。

#### 用户影响

- 后续维护可通过统一矩阵验证视觉改动没有破坏手机、平板、桌面或可展开界面的原有功能。

#### 测试验证

- `git diff --check` 通过；`tests.md` 继续只作为根索引，未改变测试文档结构。

#### 部署或回滚注意事项

- 此提交只更新手工测试文档，不影响运行时、构建产物、数据或服务部署；回滚仅会移除新增验收说明。

### 收口全屏预览与 Git 菜单的浮层层级（`c3a90b0`）

#### 问题或目标

- 旧 Git 菜单和图片预览使用 300 至 1000 的离散层级，存在盖住 Review、Diff 或模态弹窗的风险；手机 Git 菜单内的紧凑操作也未全部满足触控尺寸。

#### 实现内容

- 将登录弹窗、Git 菜单和两类图片预览收敛到统一的 modal、popover、preview 层级变量；Git 手机菜单改用动态视口高度。
- 为 Git 触发器、分支、提交、文件、重置、筛选操作和图片预览关闭按钮补齐至少 44px 的粗指针命中区。

#### 用户影响

- Git 菜单不会越级覆盖全屏 Review 或 Diff；手机端分支、提交、文件与图片预览操作更容易点按。

#### 测试验证

- `pnpm run build:frontend` 通过；`AppModals` 与 `CommandExecutionBlock` 共 2 个测试文件、8 项测试通过。
- 全仓不再存在业务浮层的三位数 `z-[...]`、`transition-all` 或 `grid-template-rows` 布局动画，`git diff --check` 通过。

#### 部署或回滚注意事项

- 不改变 Git、登录、附件或图片预览业务逻辑，不新增依赖和请求；重新构建前端即可生效。
- 回滚 `c3a90b0` 只恢复旧浮层层级与触控样式。

### 修复手机线程顶栏控件裁切（`98f4c33`）

#### 问题或目标

- `375px` 线程页中，顶栏操作区此前禁止收缩；终端与长 Git 分支按钮合计宽度超出可用空间，虽然页面整体没有横向滚动，但右侧圆角和下拉箭头会被内容容器裁掉。

#### 实现内容

- 仅在小于 `640px` 的布局中允许 `ContentHeader` 操作区收缩并保持 `min-width: 0`，让既有分支名称省略规则在可用宽度内正常工作。
- 手工验收矩阵补充 `375x812` 顶栏安全边距、完整分支按钮和终端触控宽度要求。

#### 用户影响

- 手机端长分支名称会正确省略，Git 分支按钮的图标、右圆角和下拉箭头完整可见；终端按钮仍保留至少 44px 命中宽度。

#### 测试验证

- 全量单元测试 34 个文件、377 项通过；前端与 CLI 构建通过。
- Playwright 重新执行 6 组视口与主题验收：手机浅深色顶栏 `scrollWidth=clientWidth=375`，分支按钮右边界为 `363px`，无控制台或页面错误。

#### 部署或回滚注意事项

- 不改变 `640px` 及以上布局，不新增依赖、接口或网络请求；重新构建前端即可生效。
- 回滚 `98f4c33` 会恢复旧的手机顶栏裁切行为，不影响聊天、项目或 Git 数据。

### 本次 `dev` 发布范围

- 本次推送包含上述 7 个此前未发布提交：`26ff119`、`29f8958`、`25e54ae`、`0e0e71b`、`bd7db4e`、`c3a90b0`、`98f4c33`。
- 只更新 GitHub `dev`，不合并、不推送 `main`；13511 已作为验收实例加载相同构建，若需回滚可将 `dev` 指回 `22400a0` 并重新构建目标服务。

### 修复手机技能列表自动唤起输入法（`e23f5a5`）

#### 问题或目标

- 手机端点击 Composer 的“技能”入口后，搜索框会立即获得焦点并弹出软件键盘，技能列表被压缩和遮挡；收回键盘后继续点按也容易让弹层关闭。

#### 实现内容

- 为 `ComposerSearchDropdown` 增加可选的搜索框自动聚焦控制；手机布局打开技能列表时不自动聚焦，桌面布局继续保持打开即聚焦的快速搜索体验。
- 保留搜索、技能选择、提示词创建和弹层关闭逻辑；用户主动点击搜索框后仍可正常唤起输入法并筛选。

#### 用户影响

- 手机点击“技能”后直接看到完整列表，不会自动弹出输入法；只有主动点击搜索框才出现键盘，搜索框失焦后列表仍保持打开。
- 电脑端的键盘搜索流程不变。

#### 测试验证

- `git diff --check`、`pnpm run build` 和全量 Vitest（34 个文件、377 项）通过。
- Playwright 在 `375x812` 验证初次打开不聚焦、菜单完整位于视口内，主动点击搜索可聚焦且失焦后菜单仍存在；在 `768x1024` 验证桌面布局仍自动聚焦。
- `192.168.7.249:13512` 登录后实际加载 22 个技能，点击技能入口时 `searchFocused=false`，菜单可见且中心点可命中。

#### 部署或回滚注意事项

- 仅改变前端焦点管理，不修改技能接口、数据、持久化格式或服务协议；性能路径只增加一次布尔判断，不新增请求、监听器或渲染循环。
- 回滚 `e23f5a5` 会恢复手机打开技能列表时自动弹出输入法的旧行为。

### 优化手机端聊天输入控制区（`1021ecb`）

#### 问题或目标

- 手机端同时平铺附件、权限、技能、模型、推理、语音和发送控件，空间不足时模型文字被严重截断，长占位提示还会换行挤压控制区。

#### 实现内容

- 将手机端权限与模型/推理选择整合为带真实当前模型摘要的设置胶囊，并提供独立底部设置面板；附件、技能、语音和发送继续保留为主栏快速入口。
- 设置面板复用既有权限及模型组件，修正弹层层级和 fixed 定位；手机输入提示改为短文案，主栏及面板关键触点统一为 44px。
- 桌面端继续显示原有独立权限、技能和模型/推理控件，不改变布局和操作步骤。

#### 用户影响

- `375x812` 手机宽度下，Composer 主栏保持单行、没有横向溢出，模型摘要仍可见，权限、模型和推理均可正常修改。
- 浅色和深色主题中的设置面板、权限菜单及模型菜单都位于视口内，不会被其他 UI 遮挡或无法点击。

#### 测试验证

- `pnpm run build` 和全量 Vitest（34 个文件、377 项）通过。
- Playwright 在 `375x812` 浅色/深色、`768x1024` 和 `1440x900` 验证控件可点击、菜单在视口内、无横向溢出；关键手机触点实测均为 44px。
- 浏览器性能采样确认该布局改动没有引入新的请求、无界 fanout、持续轮询或渲染循环。

#### 部署或回滚注意事项

- 仅涉及前端布局、交互和文案，不修改 HTTP、RPC、配置或持久化格式；重新构建静态资源即可部署。
- 回滚 `1021ecb` 可恢复旧的手机平铺控制区，聊天内容和用户设置不会丢失。

### 修复空项目下方隐藏聊天组占位（`dc18e71`）

#### 问题或目标

- 项目区只渲染可见项目分组，但非搜索状态曾按完整线程分组计算布局高度；项目外聊天对应的隐藏分组因此仍占位，使最后一个空项目下方出现大段空白。

#### 实现内容

- 项目布局顺序始终基于实际渲染的 `filteredGroups`；拖拽时先在完整项目顺序中完成位置投影，再过滤为可见项目顺序，保留原有拖拽排序语义。

#### 用户影响

- 空项目后方的聊天区紧随可见项目列表，隐藏聊天组不再撑出空白；桌面、手机和平板侧栏间距保持一致。

#### 测试验证

- `git diff --check`、`pnpm run build` 和全量 Vitest（34 个文件、377 项）通过。
- 13510 真实页面曾在桌面浅色/深色、`375x812` 和 `768x1024` 下确认异常间距由 106px 收敛至 10px，验证过程未改变服务 PID。

#### 部署或回滚注意事项

- 仅修改前端侧栏布局计算，不修改线程、项目数据、接口或服务配置；回滚 `dc18e71` 会重新暴露隐藏聊天组占位问题。
- 本次推送包含上述三个此前未发布提交，只更新 GitHub `dev`，不合并、不更新 `main`。

## 2026-07-20

### 为围栏代码块增加复制按钮（`ebe3476`）

#### 问题或目标

- 聊天回复和计划卡中的 YAML、命令行等围栏代码块此前只能手动全选复制，容易漏掉缩进或换行。

#### 实现内容

- 为普通回复和计划卡中的围栏代码块提供统一的复制按钮、键盘触发、无障碍标签和短暂“已复制”反馈。
- 普通回复复制解析后的原始代码；计划卡复用既有点击委托与剪贴板降级路径，不把大段代码写入 DOM data 属性。

#### 用户影响

- YAML、bash、text、其他语言和无语言代码块右上角均可一键复制；复制内容不包含语言名、按钮文字或 Markdown 围栏。

#### 测试验证

- `markdownPipeline` 聚焦测试、全量 Vitest（33 个文件、370 项）、`pnpm run build` 均已通过。
- Playwright 在桌面浅色、手机深色和 768px 视口验证普通代码块与计划卡复制、Enter 触发、反馈复位、无横向溢出及无新增 API 请求。

#### 部署或回滚注意事项

- 仅有前端展示与剪贴板交互变更，不改后端协议、服务配置或数据格式；重新构建前端资源即可部署。
- 回滚 `ebe3476` 可单独移除复制按钮，不影响聊天数据或现有代码块内容。

### 修复空项目目录残留空白（`49b45b9`）

#### 问题或目标

- 项目从有线程变为空目录后，侧边栏可能沿用旧测量高度，导致下方项目或“聊天”分区出现大段空白，刷新页面后才恢复。

#### 实现内容

- 为项目组布局增加包含项目名、折叠状态、线程状态、可见线程数和“显示更多”状态的签名；签名变化后在 DOM 更新完成时重新测量项目组高度。

#### 用户影响

- 没有任务的目录与普通列表保持紧凑间距；后续项目和“聊天”分区立即回到正确位置，无需 F5。

#### 测试验证

- `git diff --check`、`pnpm run build`、全量 Vitest（33 个文件、370 项）均已通过。
- 真实组件布局夹具在浅色和深色主题确认空目录高度为 60px，项目区至聊天分区间距为 4px。

#### 部署或回滚注意事项

- 仅修改侧边栏前端布局测量，不改线程数据、接口、路由、服务配置或端口。
- 回滚 `49b45b9` 即可恢复旧布局计算逻辑，不影响已保存的项目或聊天。

### 为聊天输入框增加斜杠命令和 Skill 补全（`49a9873`）

#### 问题或目标

- 输入框此前只有按钮式 Skill 选择和 `@` 文件补全，不能像 Codex 终端一样通过 `/` 浏览命令或通过 `$` 快速选择 Skill。

#### 实现内容

- 新增纯函数补全模型及统一 `/`、`$` 浮层，支持筛选、方向键、Enter、Tab、Esc、鼠标和触控操作。
- `/` 提供 `/model`、`/fast`、`/ide`、`/permissions` 等命令；`$` 复用既有 Skill 芯片和 app-server 输入协议，最多渲染 50 个 Skill。
- 补齐中文提示与全局暗色主题样式，不新增后端接口、配置项或 `skills/list` 请求。

#### 用户影响

- 在输入框内可直接浏览并插入 Codex 命令，或用 `$` 搜索、选择当前工作目录已加载的 Skill；占位提示同步显示 `/`、`$`、`@` 三种入口。

#### 测试验证

- 全量 Vitest（34 个文件、377 项）和 `pnpm run build` 已通过。
- Playwright 覆盖亮暗主题、桌面、375x812 手机及 768x1024 平板；30 次输入测量中 `/` 菜单 P95 为 33.1ms、`$` 菜单 P95 为 17.3ms，额外 `skills/list` 请求为 0。

#### 部署或回滚注意事项

- 为纯前端交互更新，重新构建前端资源即可部署，不迁移聊天、Skill 或配置数据。
- 回滚 `49a9873` 可移除补全入口；既有按钮式 Skill 选择和 `@` 文件补全不受影响。

### 限制斜杠补全仅在消息开头触发（`fabf85b`）

#### 问题或目标

- Codex 斜杠命令只应位于整条消息开头；此前多行提示正文中的路径或普通斜杠文本可能误触发命令菜单。

#### 实现内容

- 将 `/` token 匹配范围收紧为消息首部允许的空白之后，并补充回归测试；`$` Skill 补全逻辑保持不变。

#### 用户影响

- 只有消息开头输入 `/` 才出现命令菜单，正文和后续行的 `/` 始终作为普通文本输入。

#### 测试验证

- `composerAutocomplete` 7 项聚焦测试、`vue-tsc --noEmit` 与 `git diff --check` 均已通过。

#### 部署或回滚注意事项

- 不改后端协议、配置、数据格式或端口；如需恢复旧的宽松匹配，可单独回滚 `fabf85b`。
- 本次推送将上述四个此前未发布提交同步到 GitHub `dev`，不合并、不更新 `main`。

## 2026-07-16

### 按设备设置回车发送默认值

#### 问题或目标

- 原有新浏览器统一默认“需要 ⌘ + Enter 才发送”，不符合电脑直接按 Enter 发送、手机保留多行输入与发送按钮的使用习惯。
- 需要让手机首次访问默认开启该开关、电脑首次访问默认关闭，同时绝不能覆盖用户已经手动保存的选择。
- 本次推送包含此前尚未发布的 `381980d`，只更新 `dev`，不修改 `main`。

#### 实现内容

- 复用应用现有的 768px 手机断点：手机初始化传入 `sendWithEnter=false`，电脑传入 `sendWithEnter=true`，界面“需要 ⌘ + Enter 才发送”开关因此分别显示开启和关闭。
- 保持 `codex-web-local.send-with-enter.v1` 的原有键和 `0/1` 编码；只有键不存在时才使用设备默认值，已有值始终优先。
- 扩展偏好单测和手工验收说明，覆盖电脑、手机及两端相反的已保存值。

#### 用户影响

- 空存储的电脑直接按 Enter 发送，设置中的修饰键要求开关默认关闭。
- 空存储的手机默认要求 Command/Ctrl+Enter，设置开关默认开启，普通 Enter 可以继续用于编辑多行内容。
- 用户手动切换后的选择在刷新、重新访问及另一端的设备默认判定中都不会被覆盖；聊天宽度、提供方和其他设置不受影响。

#### 测试验证

- `useAppPreferences` 聚焦测试 7/7 通过，全量单元测试 33 个文件、369 项通过。
- `vue-tsc --noEmit`、Vite 前端构建、`tsup` CLI 构建、CLI `--help` 冒烟和 `git diff --check` 全部通过。
- Playwright 使用空存储在 `1440x900` 浅色电脑确认开关关闭，在 `375x812` 深色手机确认开关开启；两端相反的已保存值均保持，页面无横向溢出和运行错误。
- 初始化不写入 localStorage，也没有新增请求、监听器、定时器或循环；只在已有偏好读取路径增加一次 O(1) 布尔默认值选择。

#### 部署或回滚注意事项

- 不需要数据迁移；部署同一 `dev` 提交构建的前端资源即可，既有 `codex-web-local.send-with-enter.v1` 值继续有效。
- 回滚 `381980d` 并重新构建即可恢复统一修饰键默认值，不会删除聊天、草稿或偏好数据。
- 本次只推送 GitHub `dev` 分支，不合并、不更新 `main`。

### 发布 macOS 磨砂玻璃简白界面并修复任务终态竞态

#### 问题或目标

- 在不改变既有布局、协议和功能入口的前提下，将整体前端统一为 macOS 磨砂玻璃简白风，并避免为了玻璃效果制造无意义的 UI 重叠、层叠上下文和触控遮挡。
- 保证桌面、平板和手机端的菜单、弹窗、输入区、代理详情及其他可展开界面仍可点击、不会越界；特别修复手机线程时间与菜单入口互相挤占，以及开发环境与生产构建表现不一致的问题。
- 修复任务实际已经完成后，旧的异步运行态响应把主任务错误覆盖为“已中断”或让子代状态串到其他 turn、必须刷新页面才恢复的问题。
- 本次推送包含 `199211c`、`7273a81`、`3a12193` 和 `7cfdfdd` 四个此前尚未发布到 `origin/dev` 的提交，只更新 `dev`，不修改 `main`。

#### 实现内容

- `199211c` 重做 macOS 磨砂玻璃简白界面：为浅色与深色主题建立低饱和半透明表面、细边框、内高光和柔和阴影；菜单、弹窗、输入框、代码、Diff 与终端继续使用高不透明表面。首页、侧栏、顶栏、聊天、Composer、Skills、Directory、Automations、归档、Review 和各类弹层统一视觉，同时补齐粗指针触控区、弹层方向及全屏层级。
- `7273a81` 保留手机线程时间与菜单入口：在粗指针设备上让时间和 36px 菜单按钮并列常显，覆盖 hover、focus-within 与强制菜单状态，避免触屏聚焦后时间消失或标题、时间、菜单重叠。
- `3a12193` 修正生产构建中的触屏菜单位移：不再依赖会被生产 CSS 优化移除的 `translate: none`，改为稳定覆盖 Tailwind 位移变量，使开发页和生产页中的菜单按钮都完整位于线程行内。
- `7cfdfdd` 修复任务终态竞态：前端为运行态请求加入 sequence、generation 与 lifecycle epoch 校验并保存有界终态历史；服务端按真实 root/child turn 生命周期归并状态，处理 UUIDv7 时间回退、重叠 turn、迟到通知、孤儿迁移和主动中断，旧响应不能再覆盖较新的完成状态。

#### 用户影响

- 整体界面呈现统一的 macOS 简白玻璃风，但没有通过重叠面板或高成本实时模糊强行制造效果；原有导航、聊天、项目、终端、技能、自动化、设置、Diff 和 Review 功能入口保持不变。
- 375、767、768 和 1440 宽度下，可展开菜单和面板不会越界；手机线程的时间与菜单按钮同时可见，粗指针可以直接命中菜单，不依赖 hover。
- 任务完成后主推理状态会直接、稳定地显示“已完成”，无需 F5；子代继续显示自身真实终态，旧 turn 不会干扰正在运行的新 turn。
- 本次不修改 Codex Session、聊天内容、项目文件、用户偏好或接口格式，不需要迁移数据。

#### 测试验证

- UI 阶段全量单元测试 33 个文件、303 项通过；最终竞态修复后的全量单元测试 33 个文件、369 项通过，竞态定向测试 150/150 通过。
- `vue-tsc --noEmit`、Vite 前端构建、`tsup` CLI 构建、`node dist-cli/index.js --help` 和 `git diff --check` 全部通过。
- Playwright 在 Light/Dark 主题及 1440、768、767、375 宽度下完成响应式验收；项目/线程/整理菜单、设置、Composer、Git、终端、自动化、Skill/Directory、图片、Diff、Review、代理进度和全屏输入均经过真实点击，未发现横向溢出、页面错误或不可点击遮挡。
- 任务终态回归在 `1440x900` 浅色桌面与 `375x812` 深色手机上确认主状态无需刷新即可从“已中断”恢复为“已完成”，子代保持真实“已中断”，桌面详情按钮和手机底部面板均可点击。
- 性能审计确认没有新增重复请求、无界 fanout、持续动画或 `backdrop-filter`；终态历史前端每线程最多 32 条、服务端每根线程最多 64 条，子代映射继续受 `nodeLimit` 限制。
- `192.168.7.249:13511` 已使用本次最新构建运行；密码登录和登录后页面均返回 HTTP 200，认证后的首页文件与本地 `dist/index.html` 哈希完全一致。

#### 部署或回滚注意事项

- 前后端运行态保护与 UI 资源应使用同一 `dev` 提交完整构建后部署；本次不改变 HTTP、WebSocket、SSE、RPC method、CLI 参数或持久化格式。
- 13511 开发验收实例已经升级；正式控制台 13510、持久开发服务 5173 和临时验收服务 4173 均未因本次推送重启或替换。
- 如需回滚，应从 `7cfdfdd`、`3a12193`、`7273a81` 到 `199211c` 按逆序逐项回退并重新构建；只回退手机定位修复会重新暴露触控位移问题，只回退终态保护会恢复必须刷新页面的竞态。
- 本次只推送 GitHub `dev` 分支，不合并、不更新 `main`。

### 移除过时的公开使用文档

#### 问题或目标

- 仓库现有 `README.md` 与静态网站仍包含已经过时的安装、部署和旧项目介绍，不再适合当前自用项目。
- 清理 GitHub 默认分支与开发分支中的公开使用文档，同时避免把 `dev` 的其他功能改动带入 `main`。

#### 实现内容

- 删除 318 行的 `README.md` 和 1300 行的 `docs/index.html`。
- 删除仅供上述公开文档使用的四张界面截图：`chat.png`、`chat-mobile.png`、`skills-hub.png` 与 `skills-hub-mobile.png`。
- 保留 `AGENTS.md`、`PROJECT_SPEC.md`、`CHANGELOG.md`、app-server 协议资料、内部 LLM Wiki 和测试资料。

#### 用户影响

- GitHub 仓库首页不再展示旧版快速开始、安装、部署和功能介绍。
- 旧静态项目网站及其配套截图不再保留；应用本身的界面和运行行为不变。

#### 测试验证

- `git diff --check` 通过。
- 逐项核对删除前的六个文件在 `origin/main` 与 `dev` 中 blob 完全一致，可独立同步删除，不会带入其他代码差异。
- 本次仅删除文档与图片素材，不涉及运行代码，因此不需要执行构建或性能测试。

#### 部署或回滚注意事项

- 不需要重新构建、重启服务或迁移数据，现有部署不受影响。
- 如需恢复，可单独回退本次文档删除提交；恢复不会改变任何应用配置或用户数据。

### 完善主推理模型、子代理真实模型信息与聊天输入体验

#### 问题或目标

- 将主代理从子代理详情树中移到进度卡片外层，使用“主推理模型”表达其职责，并在标题下方直接展示当前模型、思考强度和速度。
- 为每个子代理展示其自身真实的模型信息，禁止复用主模型、其他子代理或写死的占位值；同时保证实时运行、页面刷新和同一子线程后续复用时信息一致。
- 修复聊天输入内容增多后仍停留在接近单行高度、文字被底部控制区挤压隐藏的问题，让输入框随内容增长并在合理上限后切换为滚动。
- 本次推送包含 `aac95f3`、`e31a0b6` 和 `0e0f8d8` 三个此前尚未发布到 `origin/dev` 的提交，只更新 `dev`，不修改 `main`。

#### 实现内容

- `aac95f3` 调整主推理模型展示层级：将主模型从子代理树中移出，进度卡片顶部固定显示“主推理模型”、当前状态以及 `Model`、`Thinking`、`Speed`；展开代理详情后只呈现真实子代理节点。
- `e31a0b6` 显示子代理自身的模型信息：从每个子代理自己的 rollout `turn_context` 读取真实 `model` 与 `effort`，按线程隔离写入进度节点；会话前缀读取限制为 512 KiB、文件并发限制为 8，并使用有界缓存支持刷新恢复和后续 turn 复用同一子线程。Codex 当前未持久化可验证的子代理速度字段，因此不会伪造 `Speed: Standard` 或 `Speed: Fast`。
- `0e0f8d8` 让聊天输入框随内容自动增高：复用现有 `nextTick` 测量队列，根据 textarea 的 `scrollHeight` 在 44px 至 160px 间动态调整高度；达到上限后启用内部滚动和原有全屏入口，缩短或清空草稿时同步收缩。

#### 用户影响

- 主推理模型和子代理层级更清晰，子代理展开区不再重复显示主代理。
- 每个子代理下方显示其自身真实的 `Model` 与 `Thinking`；协议没有提供的速度信息保持不显示，不会误导用户。
- 输入多行或自动换行文字时，聊天框会向上增长并持续显示内容；极长草稿仍可滚动或切换全屏，不影响 Enter 发送、附件、技能、模型和上下文控制。

#### 测试验证

- 最新分支完整单元测试 33 个文件、303 项全部通过；`vue-tsc --noEmit`、Vite 前端构建、`tsup` CLI 构建和 CLI `--help` 冒烟全部通过。
- 真实线程的 `/codex-api/agent-progress` 返回三个子代理各自的 `gpt-5.6-sol` 与 `ultra`；明暗主题页面均逐行显示 `Model: gpt-5.6-sol · Thinking: ultra`，没有从主推理模型复制 `Speed: Fast`。
- 子代理回归测试覆盖不同子线程值不串线，以及 rollout 删除后同一子线程在后续 root turn 中仍从已验证缓存恢复自身详情。
- 聊天输入框在 `1440x650` 明暗主题下实测从 44px 增长到 96px，超长文本在 160px 后启用滚动和全屏按钮，清空后恢复 44px；调整高度期间没有新增 `/codex-api/` 请求。
- 开发验收实例 `192.168.7.249:13511` 使用本次构建运行并返回 HTTP 200；正式控制台 `13510` 未重启、未替换。

#### 部署或回滚注意事项

- 部署时应使用同一 `dev` 提交重新生成 `dist` 与 `dist-cli`；不需要迁移 Codex Session、线程偏好、草稿或其他本地数据。
- 子代理模型详情依赖其自身 rollout；读取不到真实数据时保持空白是预期的诚实降级，不应改为主模型兜底。
- 可分别回滚上述三个离散提交；回滚 `e31a0b6` 只会移除子代理模型详情，回滚 `0e0f8d8` 会恢复固定高度输入框，均不会删除聊天数据。
- 本次只推送 GitHub `dev` 分支，不合并、不更新 `main`。

### 发布 `dev` 分支的收尾修复与默认配置

#### 问题或目标

- 将绞杀式前端拆分成果作为独立 `dev` 分支首次上传，供后续开发与测试使用，不修改或推送 `main`。
- 修复多代理总进度已经完成、子代仍显示运行中且进度停在 `0/N`，必须刷新页面才恢复的问题。
- 将当前产品默认行为收敛为 Enter 发送、宽聊天布局和仅保留 Codex 提供方，并为活动聊天补充不经过归档的永久删除入口。
- 本次首次推送包含 `origin/main..dev` 的全部提交；`04532fa` 至 `62faba7` 的分层重构与验证项以及记录提交 `20a5901` 已在下方“采用绞杀式拆分收敛前端与 app-server bridge”中逐项说明，本节补充其后的三个功能修复。

#### 实现内容

- `9c2694a` 修复子代理完成状态被尾随通知覆盖：任务终态到达后不再被较晚的运行中通知回写，父级完成状态、子代状态和步骤计数可以实时一致收敛，无需 F5。
- `f912014` 仅保留 Codex 提供方并调整聊天默认设置：Enter 发送默认开启，聊天宽度默认使用宽布局；移除 OpenCode Zen、OpenRouter 等非 Codex 提供方的前后端选择、代理和兼容代码，既有 Codex 配置路径保持不变。
- `85d49d6` 在线程菜单增加永久删除：在“归档聊天”下方增加带二次确认的“永久删除”，直接调用 `thread/delete`，不调用 `thread/archive`；成功后清理线程状态并切换相邻聊天，失败时保留聊天、选择并恢复原置顶状态。

#### 用户影响

- 多代理任务结束后，父级与子代状态会立即显示完成，步骤进度不再停留在零。
- 新用户和未保存相关偏好的用户默认使用 Enter 发送与宽聊天区；提供方界面只显示 Codex，减少无效选项和分支代码。
- 用户可以从活动聊天菜单直接永久删除聊天；取消确认不会发送请求，确认后聊天不会出现在归档列表。永久删除不可撤销。

#### 测试验证

- 最新分支全量单元测试 32 个文件、298 项全部通过；`vue-tsc --noEmit`、Vite 前端构建和 `tsup` CLI 构建全部通过。
- 永久删除聚焦测试确认成功路径只调用 `permanentlyDeleteThread`、从不调用 `archiveThread`，并覆盖请求等待期间切换线程的竞态及失败时保留状态。
- Playwright 在 `1280x900` 明暗主题下确认菜单顺序为“归档聊天”后紧跟“永久删除”；取消路径请求数为零，确认路径为一次 `thread/delete`、零次 `thread/archive`、零次额外线程列表刷新，置顶删除失败后会恢复原置顶集合。
- 开发测试实例 `13511` 使用当前构建启动并返回 HTTP 200；真实页面已确认新菜单和确认框可见，取消时没有破坏性 RPC。

#### 部署或回滚注意事项

- 本次只创建并推送 `dev`，不合并、不更新 `main`；部署测试环境时应从同一 `dev` 提交构建 `dist` 与 `dist-cli`。
- 从旧配置升级后，非 Codex 提供方入口和代理代码不再可用；需要这些提供方时应回滚 `f912014`，不要只恢复界面选项。
- `thread/delete` 是 Codex 原生不可逆操作；回滚界面代码不会恢复已经永久删除的聊天。回滚其余前端拆分不需要迁移 Codex Session 或本地偏好数据。

## 2026-07-15

### 采用绞杀式拆分收敛前端与 app-server bridge

#### 问题或目标

- `App.vue`、`ThreadConversation.vue`、`useDesktopState.ts`、`codexGateway.ts` 和 `codexAppServerBridge.ts` 长期集中了过多协调职责，修改一个领域时容易误伤实时通知、线程恢复、项目传输或渲染协议。
- 本阶段不推倒重写，而是先使用特征与契约测试固定行为，再以 facade 保留原入口、逐个抽出纯逻辑和领域控制器，最后整理 app-server 子进程边界。
- 同时治理长会话的两个明确性能风险：折叠命令的大文本节点仍常驻 DOM，以及单个历史 `commandExecution.aggregatedOutput` 可以无界增长。

#### 实现内容

- `04532fa` 拆分线程运行态轮询控制器：将运行线程 ID 汇总、单 timer、in-flight 门禁、前台/联网唤醒、代际隔离和 UUIDv7 旧终态防护移出 `useDesktopState`，保留 2 秒活动与 15 秒空闲校验语义。
- `d77917a` 抽取命令执行消息组件：用 `CommandExecutionBlock` 和纯展示模型统一时间线、命令组与 worked 详情中的三套重复 DOM，并保留原状态、输出文本和 ARIA 契约。
- `4e3c7fb` 收缩应用壳：抽出 `useAppPreferences`、Codex 登录弹窗和项目 ZIP 导出弹窗，保留七个 `localStorage` 键、原 props/emits、焦点和错误反馈行为。
- `44f08af` 按需挂载折叠命令输出：折叠时不再创建 `<pre>` 及其大文本节点，展开时才挂载完整输出，折叠/展开交互与原始消息数据不变。
- `79cde62` 抽出实时增量批处理：将代理文本、命令输出和推理文本三通道统一到一个 180 ms 控制器，保持 512/256/128 KiB 尾部限额、顺序和清理时机。
- `d0edd24` 修复弹窗暗色作用域：将 scoped CSS 中不稳定的暗色后代选择器改为整段全局选择器，确保登录、ZIP、输入框、按钮和错误面在暗色页面上使用深色表面。
- `a3cbf9c` 抽取项目工作区前端网关：将工作区根目录缓存、目录浏览、项目打开、ZIP、GitHub 克隆、项目与文件搜索移入 `projectGateway`，`codexGateway` 继续重导出原函数与类型。
- `35b51fe` 限制历史命令输出响应体：在普通 thread RPC、历史分页、live-state、Session 恢复和快照回退路径统一保留每项最新 256 KiB UTF-8 尾部，并添加截断标记和原始字节数；正常输出逐字不变。
- `1809417` 拆分线程列表加载控制器：封装首屏 single-flight、2 秒复用窗口、10 秒后台串行分页、活动任务暂停、项目分组合并和归档剔除，保留 `thread/list` 游标与排序。
- `c67b2ad` 下沉项目 ZIP 传输流程：用 `useProjectZipTransfer` 封装导出进度、下载/分享错误映射、隐藏文件输入、导入互斥与根目录/线程/默认项目名刷新顺序，`App.vue` 只保留应用级编排。
- `65f131d` 拆分会话 Markdown 渲染管线：将路径/内联 token、块级语法、HTML 输出和共享类型抽到 `markdownInline`、`markdownBlocks`、`markdownHtml` 和 `markdownTypes`，保留文件链接、列表、表格、代码高亮与 LRU 缓存行为。
- `4e8ea58` 说明历史输出限额的两个阶段：标注 snapshot/扫描前限额与 Session/通知合并后限额的不同保护目的，避免后续将幂等防线误当重复代码删除。
- `aa10851` 抽取 app-server JSONL transport：封装惰性单进程启动、UTF-8 分块换行组帧、generation 隔离、精确 JSON 行写入、显式停止及 SIGTERM/1500 ms SIGKILL 兜底，`AppServerProcess` 保留 RPC、通知与业务生命周期门面。
- `28b96fa` 补充五份分层重构手工验收文档：增加折叠命令无 `<pre>`、256 KiB UTF-8 限额、线程分页去重、ZIP 单请求流程和 TestChat 链接契约的前置条件、操作、预期与清理步骤。
- `a35f23c` 修复 transport 异常退出清理：使用 `try/finally` 保证 bridge 或通知监听器抛错时仍按 child 身份与 generation 双重条件清理旧运行态，后续可正常重启。
- `62faba7` 校准手工验收语义：分开“浏览器不支持分享”与“权限阻止分享”的提示，并将后台分页等待改为首屏响应后的两个完整十秒间隔及各页响应。

#### 用户影响

- 日常路由、发送、恢复、实时消息、项目 ZIP、登录、项目搜索、线程排序和 Markdown 显示保持原行为；本阶段没有更换 Vue、Vite、Express 或 Codex app-server 技术栈。
- 折叠命令不再常驻大输出 DOM；超大历史命令显示截断标记与最新 256 KiB 尾部，普通输出不变。
- 登录与 ZIP 弹窗在浅色和深色主题下保持可读；既有本地偏好、Session、项目 ZIP 和 Codex 配置不需迁移。

#### 测试验证

- 全量单元测试 34 个文件、321 项全部通过；`pnpm exec vue-tsc --noEmit`、前端/CLI 完整构建、`node dist-cli/index.js --help` 和 `git diff --check` 全部通过。
- 浏览器验收使用 `1440x1000` 视口：TestChat 唯一标记的文件链接在明暗主题下均满足 `hrefOk/titleOk/textOk/target/rel`；协议 fixture 下命令块满足折叠 `pre=0`、展开 `pre=1`、再折叠 `pre=0`；ZIP 弹窗单次 GET 并完成明暗主题截图，Codex 登录弹窗的链接、回调输入、禁用提交和取消状态均正常。
- 最终性能门禁复跑中，主页 `thread/list=1`、`totalApiKB=102.9`、`warnings=[]`；目标线程 `threadReadWithTurns=1`、`duplicateKeys=0`、`warnings=[]`、`totalApiKB=417.1`。更早的同一实时数据对比样本中，当前分支为 423.2 KiB，基线为 418.0 KiB。早期一次额外读取已用 trace 定位为 WebSocket `responseStreamDisconnected` 重连事件在 220 ms debounce 后触发的一致性刷新，基线无该事件且后续复跑未复现，因此保留刷新逻辑。
- 最终 Vite 主包 580.82 kB（gzip 179.97 kB），会话懒加载 chunk 86.01 kB（gzip 25.15 kB）；新控制器保持单 timer、单 in-flight 或串行请求，未引入无界扇出或新的全局缓存。

#### 部署或回滚注意事项

- HTTP URL、WebSocket/SSE、RPC method 与既有 payload、CLI 参数、`localStorage` 键和 Codex Session 持久化格式不变，不需要数据迁移。
- 本阶段同时包含前端渲染与 bridge/transport 保护，部署时应使用同一提交生成的 `dist` 与 `dist-cli`；回滚可按上述离散提交的领域边界逐项回退。
- 256 KiB 限额只作用于返回给 WebUI 的历史命令输出，不重写原 Session；回滚限额不会恢复或删除任何会话数据。

### 新增真实的主代理与多级子代理进度展示

#### 问题或目标

- 原有聊天界面只能显示笼统的 Thinking 状态。模型调用多个子代理或子子代理时，用户无法判断代理是否已经派发、正在执行、等待结果、已完成还是失去连接，长时间静默容易被误认为卡死。
- 刷新页面、重新进入历史聊天或实时通知短暂中断后，代理层级、阶段和耗时需要从后端状态恢复，不能依赖当前页面内存或伪造进度。
- 多代理任务完成后，卡片中的耗时仍会继续增长；完整代理树又默认展开，占用过多聊天空间。

#### 实现内容

- 新增 `AgentProgressTracker`，从 Codex app-server 的真实线程、turn、子代理与通信事件维护根任务阶段、代理父子关系、状态、活动时间、结果可用性和连接健康状态。
- 新增代理进度查询与按需结果读取路径，前端通过 `useDesktopState` 恢复实时或历史进度；恢复请求增加重试节流、加载代次和 epoch 校验，过期请求不能覆盖当前线程的新状态。
- 新增 `TurnProgressCard`：展示主代理、多个子代理及子子代理的真实树形层级、当前阶段、活动/完成计数、连接警告、模型与推理强度，并支持按需查看代理结果和独立时间线。
- 桌面端代理详情默认折叠，可独立展开或收起；时间线保持单独控制。移动端继续使用带焦点管理的底部面板，并覆盖明暗主题和窄屏布局。
- 运行中的任务继续按秒显示实时耗时；完成、失败或中断后，根任务使用 `turnCompleted` 时间、子代理使用 `completedAtMs` 或最后活动时间冻结耗时，同时停止无意义的计时器刷新。
- 修复历史多代理任务恢复时耗时起点不准确、恢复轮询过密，以及失效请求晚到后污染当前进度的问题。
- 本主题覆盖提交：`89a3fe9`、`2116ddb`、`ffc9d61`、`f964242`、`1fc691c`、`7ea3cc7`、`ded34ff`。

#### 用户影响

- 调用一个或多个子代理时，聊天中会直接呈现真实代理拓扑和推进状态；四个、五个、六个代理及多级嵌套均使用同一套交互。
- 用户可以区分正常思考、等待子代理、静默代理、连接异常和已经结束的任务，不再只看到缺少上下文的 Thinking。
- 刷新或重新进入聊天后仍可恢复代理进度；任务结束后耗时保持固定，默认折叠卡片减少聊天区域占用。

#### 测试验证

- 新增并扩展 `agentProgressTracker.test.ts`、`turnProgressUtils.test.ts` 和 `useDesktopState.test.ts`，覆盖真实事件映射、层级恢复、终态冻结、运行态计时、重试节流和失效请求隔离。
- 全量单元测试共 21 个测试文件、232 项全部通过；`vue-tsc --noEmit`、前端 Vite 构建、CLI `tsup` 构建和 CLI `--help` 冒烟全部通过。
- 使用真实 Codex 多代理线程完成桌面明色、平板和 `375x812` 移动暗色验收；正式 `13510` 页面中终态耗时等待 3.2 秒前后保持不变，代理详情、独立时间线和移动底部面板均正常，页面错误为 0。
- 浏览器性能报告中没有新增 API 请求或重复请求；与修改前基线相比，重复请求计数一致，采样 API 体积未增加。

#### 部署或回滚注意事项

- 本功能同时涉及服务端代理进度追踪、HTTP/RPC 接入和前端展示，应部署同一提交生成的 `dist` 与 `dist-cli`，避免只更新一侧导致历史恢复能力不一致。
- 代理进度、事件和结果均设有数量上限，折叠只影响展示，不会额外触发结果下载；结果仍在用户点击后按需读取。
- 回滚时应整体回退本主题的前后端提交并重新构建，不需要迁移或修改 Codex 原始 Session；回滚后只会失去新的代理进度展示，不会删除聊天正文。

### 修复首次进入聊天出现空思考强度卡

#### 问题或目标

- 关闭网页后重新进入 WebUI、首次点击聊天时，页面偶发只显示一个推理强度标识；必须刷新浏览器或切换到其他聊天再返回，完整消息才会展开。
- 该空卡来自线程首次加载期间的临时状态被当作有效实时覆盖层，用户会误以为聊天仍在思考或界面卡住。

#### 实现内容

- 收紧聊天首次加载时实时覆盖层的生成条件，只在当前 turn 确实处于运行态且存在有效活动内容时显示思考卡。
- 调整线程读取与恢复后的状态收敛，避免旧线程的模型/推理强度元数据单独形成无正文卡片。
- 增加首次进入、切换聊天、刷新恢复和无活动详情等场景的状态测试与手动验收说明。
- 本主题覆盖提交：`069e627`。

#### 用户影响

- 重新打开 WebUI 并首次进入聊天时，会直接显示完整历史内容；已结束聊天不会再残留只有推理强度的空 Thinking 卡。
- 正在运行的真实任务仍保留实时思考和代理进度，不影响模型、推理强度或发送行为。

#### 测试验证

- `useDesktopState.test.ts` 覆盖首次进入与覆盖层判定，相关线程加载手册补充刷新和聊天切换步骤。
- 本修复随本次推送一起通过 232 项全量单元测试、类型检查、生产构建和真实浏览器线程进入验收。

#### 部署或回滚注意事项

- 改动只调整前端线程状态判定，不修改 Codex Session、模型偏好或消息格式。
- 回滚不会造成数据丢失，但旧版首次进入聊天的空卡问题会重新出现。

### 修复聊天置顶、归档与菜单状态刷新后失效

#### 问题或目标

- 原有“固定线程”操作只在当前页面内存中有效，刷新页面后立即失效，名称也不符合用户对聊天操作的理解。
- 置顶、归档、删除和侧栏菜单之间缺少统一状态收敛，已经归档或删除的线程可能继续残留在置顶列表。

#### 实现内容

- 将操作文案和菜单语义调整为“置顶此聊天”，并通过后端持久化接口保存置顶线程集合，使状态跨刷新、重新进入和服务重启恢复。
- 重构侧栏置顶分组与更新工具，统一置顶切换、并发线程映射、归档恢复和删除后的清理行为。
- 归档线程不再错误保留在普通置顶分组；永久删除线程后同步清理对应置顶记录，避免花架子状态和孤儿 ID。
- 新增聊天入口与菜单持久化、置顶跨刷新、归档一致性和侧栏删除确认的手动验收文档。
- 本主题覆盖提交：`a15bd34`、`51469be`、`624f2d2`。

#### 用户影响

- “置顶此聊天”在刷新页面、重新打开浏览器和切换聊天后仍然有效。
- 归档、恢复、删除和置顶之间保持一致，侧栏不会继续显示已经归档或删除的置顶聊天。
- 既有聊天内容、归档数据和 Codex Session 不受影响。

#### 测试验证

- 扩展 `codexGateway.test.ts`、`pinnedThreadUtils.test.ts` 和 `useDesktopState.test.ts`，覆盖持久化读取、置顶切换、并发映射、归档和删除清理。
- 新增 `chat-menu-actions-and-persistence.md`、`pinned-chats-persist-across-reload.md` 等手动验收步骤，并随本次推送通过全量单测、类型检查和生产构建。

#### 部署或回滚注意事项

- 部署后端持久化接口与前端侧栏时应保持版本一致；已有置顶状态会继续保留，不需要人工迁移。
- 回滚代码前建议记录当前置顶列表并在回滚后刷新核对；回滚不会删除聊天或归档记录，但旧版可能无法继续使用新的持久化行为。

## 2026-07-12

### 普通 HTTP 与 JSON-RPC 请求增加统一超时

#### 问题与目标

- 原有浏览器端大量普通 HTTP 和 `/codex-api/rpc` 请求直接调用 `fetch()`，SSH 隧道中断、Node 服务异常、Codex app-server 无响应或响应体长期不结束时，页面可能永久停留在加载状态。
- Provider models 使用独立的 5 秒 `AbortSignal.timeout()`，文件上传又维护一套 60 秒手写 `AbortController`，不同功能的超时、取消和网络错误语义不一致。
- 本次改动统一浏览器请求层，同时避免用短超时中断 Git、ZIP、Skills、Composio、账户验证和 Codex turn 等正常长操作。

#### 核心实现

- 新增 `src/api/requestClient.ts`，统一管理请求建立、响应体读取、timeout timer、调用方 `AbortSignal` 和资源清理。
- 默认普通 HTTP 请求超时为 15 秒，普通 JSON-RPC 请求超时为 30 秒，长操作超时为 120 秒；文件上传继续保持 60 秒，Provider models 继续保持 5 秒快速回退。
- 新增 `timeout` 和 `aborted` 两类 `CodexApiError` 错误码，明确区分请求超时、用户主动取消和普通网络失败。
- timeout 按请求总时长计算，覆盖响应头和响应体读取；成功、失败、超时和主动取消路径都会清理 timer 与监听器。
- timeout 只表示浏览器停止等待，不自动重试，也不声称 Git、导入、clone、worktree、Skills 或 Codex 等服务端副作用已经停止。
- WebSocket、SSE、心跳、sequence replay 和重连逻辑保持原样，不套用普通 HTTP deadline。

#### 接入范围

- `rpcCall`、RPC method/notification catalog、server request 回复和 pending request 查询统一使用 RPC timeout。
- Gateway 中的线程状态、自动化、运行配置、终端、模型偏好、工作区、Git、Review、本地目录、Telegram、prompts 等普通 HTTP 请求统一接入请求工具。
- `thread/start-turn`、大型 thread 读取、ZIP 导入导出、GitHub clone、worktree、Git checkout/reset、Review、Skills 搜索/安装/同步、GitHub 登录完成、Composio、账户验证和语音转录使用长操作策略。
- Skills Hub、Skill 详情、GitHub Skills Sync 和听写转录中绕过 Gateway 的直接请求也已迁移。
- ZIP 下载保留流式进度处理；听写继续支持用户主动取消；上传继续保留 `FormData` 自动边界处理。

#### 用户影响

- SSH 隧道或后端服务异常时，普通页面请求不再永久转圈，而会在约 15 秒后结束等待并显示明确的 timeout 信息。
- RPC 通信异常会在约 30 秒后返回带方法名的 timeout 错误，便于判断是哪个 Codex 操作未响应。
- 正常的长时间 Git、ZIP、Skills、Composio、账户同步及 Codex 操作不会被 15 秒普通请求超时误伤。
- 用户取消听写会继续被识别为主动取消，而不是显示为网络失败或 timeout。

#### 测试验证

- 新增 `src/api/requestClient.test.ts`，覆盖普通/RPC/长操作 timeout、显式毫秒覆盖、提前取消、运行中取消、网络错误分类、响应体读取超时、并发请求隔离以及 timer 清理。
- timeout 与 Gateway 窄测试共 26 项全部通过。
- TypeScript/Vue 类型检查、Vite 前端生产构建、CLI `tsup` 构建和 `git diff --check` 全部通过。
- 真实浏览器运行验证中，刻意挂起 `/codex-api/skills-hub` 后约 17.7 秒观察到 `/codex-api/skills-hub timed out after 15000ms`，加载状态自动消失；快速成功请求仍正常显示空状态。
- 全量单元测试中 201 项通过，另有 2 项 Windows 文件权限位断言失败：Windows 返回 `0666`，测试期望 POSIX `0600`；该失败与本次请求超时改动无关。

#### 部署与回滚注意事项

- 本次改动仅影响浏览器端请求等待和错误分类，不修改 Codex CLI API/Key、Session、聊天文件或服务端数据格式。
- 有副作用的操作 timeout 后应先刷新并检查实际状态，再由用户决定是否重试，避免重复创建 turn、worktree、clone 或同步任务。
- 回滚时可恢复原请求调用代码并删除 `src/api/requestClient.ts`；不会留下需要迁移或清理的持久化数据。

## 2026-07-10

### 修复任务已经结束但 WebUI 持续转圈

#### 问题与原因

- 原有前端把 `turn/completed` 实时通知当作停止转圈的必要条件。浏览器休眠、网络切换、WebSocket 半开或通知丢失后，即使 Codex 已完成任务，本地 `inProgress` 仍可能永久保留。
- 同一任务由一个 WebUI 实例启动时，其他实例自己的 Codex app-server 可能把该 turn 返回为 `interrupted` 或 `notLoaded`，因此仅轮询 `thread/read` 会把仍在运行的任务误判为结束。
- 旧版跨实例活动记录允许过期租约覆盖 Session 中已经存在的 `task_complete`，会把完成任务重新显示成运行中。
- 点击停止时，如果 Codex 返回 `no active turn to interrupt`，旧逻辑只显示错误，不会清理已经过期的 UI 状态。

#### 核心实现

- 新增独立的 `ThreadRuntimeState` 服务端模块，按 `turnId` 管理状态，不再使用线程级黏性布尔值作为权威依据。
- 对 Codex Session JSONL 进行结构化增量解析，识别 `event_msg.task_started` 和 `event_msg.task_complete`，避免反复完整解析大型会话文件。
- 状态判断以最新 turn 为单位：同一 turn 的 `task_complete` 始终是不可被租约覆盖的终态；更新的本地或外部 turn 又不会被旧 turn 的完成记录误清除。
- 每个 WebUI 实例使用独立 UUID、进程身份、端口、turnId 和心跳时间写入原子租约文件。外部租约只用于证明所属实例仍存活，10 秒没有心跳后自动失效。
- 新增批量接口 `POST /codex-api/thread-runtime-state`。该接口独立返回权威运行态，不篡改 Codex 原始 `thread/list`、`thread/read` 或 `thread/resume` 响应。
- Codex app-server 的 `turn/start`、`turn/started`、`turn/completed`、异常退出和主动停止均接入统一运行态管理。

#### 前端行为

- 运行中每 2 秒批量校验一次运行态，空闲时每 15 秒校验一次；窗口重新聚焦、页面恢复可见、网络恢复和通知流重连时立即校验。
- 权威状态为完成或中断时，自动清除输入框停止按钮、侧栏转圈、activeTurnId、实时推理文本和命令状态，并强制加载最终消息。
- 权威状态为外部实例运行时，即使当前实例的 `thread/read` 返回 `interrupted`，界面仍保持正确的运行状态。
- 事件驱动刷新失败后会重新排队并延迟重试，不再清空待刷新集合后静默丢失。
- 停止操作返回 `no active turn` 时会立即重新读取权威运行态；任务已经结束则自动恢复 UI，不再留下 502 错误和永久转圈。
- 新 turn 启动期间增加乐观状态保护，防止上一 turn 的终态在启动请求尚未完成时误清除新任务。

#### 连接健康检查

- WebSocket 服务端每 10 秒发送应用心跳并执行 ping/pong 检查，无响应连接会被主动终止。
- 浏览器端增加 25 秒无帧看门狗，半开连接会主动关闭并重连，随后立即执行运行态校验。

#### 验证结果

- 全量单元测试：17 个测试文件、193 项测试全部通过。
- TypeScript 类型检查、Vite 前端生产构建和 CLI 构建全部通过。
- 双实例真实验收中，所属浏览器被刻意丢弃 `turn/completed`，旁观实例的原始 `thread/read` 同时返回 `interrupted`；两个界面仍正确显示运行，并在 Session 完成后约 2 秒内自动停止并显示最终回复。
- 所属 WebUI 进程被 `SIGKILL` 后，旁观实例在 10 秒租约到期后自动收敛为 `interrupted`，未出现永久转圈。
- 验收期间未发现浏览器控制台错误，临时服务、测试会话和测试租约均已清理。

#### 部署与回滚

- 多实例必须全部升级到本版本，才能共同写入和读取新的 turn 运行态租约；未升级实例仍可运行，但不会提供新租约。
- 运行态文件位于 `${CODEX_HOME}/linux-codex-webui-runtime/thread-runtime-leases/`，仅保存实例和 turn 活动元数据，不保存消息、API Key 或聊天正文。
- 回滚代码不会修改 Codex Session；残留租约超过有效期后不会继续被新版本视为运行中。

### 每个聊天独立保存模型和推理强度

#### 更新内容

- 模型和推理强度改为按 thread 独立持久化，不再只依赖当前浏览器内存或全局 Codex 默认配置。
- 刷新页面、切换到其他聊天后再返回、重新打开浏览器或重启 VPS，已有聊天都会恢复各自最后一次选择。
- 只有新建聊天使用 Codex CLI 当前默认模型和默认推理强度。
- 用户仍可在任意已结束聊天中随时切换模型和推理强度，下一次发送会使用该聊天保存的配置。
- 后端新增线程模型偏好持久化接口，并通过串行写入避免快速切换时旧请求覆盖新选择。

#### 验证结果

- 覆盖刷新恢复、聊天切换、模型目录暂时不完整、`thread/read` 返回旧模型以及快速连续修改等场景。
- 保留官方推理强度名称与模型支持范围，不改变 Codex CLI 实际接收的参数值。

### 新增已归档聊天管理

#### 更新内容

- 左侧栏顶部新增“已归档”入口，并新增独立归档页面。
- 支持归档聊天搜索、刷新、分页加载和数量显示。
- 支持将聊天恢复到正常列表。
- 支持永久删除归档聊天，并提供二次确认，降低误删除风险。
- 恢复或删除后会同步刷新左侧聊天列表。

#### 兼容性说明

- 归档功能仅移植归档管理本身，没有引入此前已废弃的线程活动覆盖方案。
- 每聊天模型与推理强度持久化逻辑保持不变。
